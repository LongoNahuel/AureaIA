# Sesión 2026-09-21 — Validación del backend

Objetivo: validar el backend de punta a punta antes de seguir agregando
funcionalidad, y dejar un plan por fases donde cada fase es un commit atómico.

Participantes: Daniel + Claude (Opus 5).

---

## Fase 0 — Entorno reproducible, bitácora y línea base

### Qué se hizo

- Se creó `sesiones/` (este archivo) y se lo **sacó de `.gitignore`**. Estaba
  ignorado desde `9e278f7` como "bitácoras locales de trabajo" y se re-confirmó
  hoy en `73803e5`. Decisión de esta sesión: las bitácoras se versionan, porque
  son la trazabilidad del trabajo sobre el backend y tienen que viajar con el
  repo.
- `requirements-dev.txt` nuevo, espejo del extra `[dev]` de `pyproject.toml`
  (`pytest`, `pytest-cov`, `pytest-qt`, `ruff`), con `-r requirements.txt`.
- Nota en `requirements.txt` fijando que la fuente de verdad del runtime es
  `pyproject.toml` y que este archivo es su espejo — apareció ayer en `31103a6`
  duplicando las deps, y sin la nota las dos listas divergen sin que nadie se
  entere.
- `venv/bin/pip install -e ".[dev]"`: el venv tenía las deps de runtime pero
  **ninguna de desarrollo**, y el paquete no estaba instalado en editable. Hasta
  hoy no se podía correr ni un test ni el lint en esta máquina.

### Línea base: estaba ROJA

Primera corrida de la suite completa después de instalar las dev deps:

| Chequeo | Resultado inicial |
|---|---|
| `ruff check` | ✅ pasa |
| `ruff format --check` | ❌ **4 archivos sin formatear** |
| `pytest -m "not integration" --cov` | ❌ **1 test falla**, 279 pasan, cobertura 81.04% |
| `pytest -m integration` | ✅ pasa (1 test) |
| `--smoke` | ✅ pasa (los 4 analizadores con modelos reales) |

Las dos fallas vienen del mismo commit, `7f6d5ff` ("feat: preparar demo VMS y
overlays inteligentes", de ayer). **El CI de `main` está rojo desde ese commit.**

**1. `ruff format --check` — 4 archivos.** `core/analytics/door_state_analyzer.py`,
`models/alarm_rule.py`, `ui/dialogs/door_state_config_dialog.py`,
`ui/widgets/video_tile.py`. Todos tocados por `7f6d5ff`; el commit no pasó por el
formateador. Corregido con `ruff format` (solo reformateo, cero cambio de
comportamiento).

**2. `test_analytics_worker_loop.py::test_worker_publica_detecciones_y_libera_todo`.**
`7f6d5ff` cambió las tres llamadas del `AnalyticsWorker` al `StreamManager` para
pasar explícitamente el flujo `"main"`:

```
stream_manager.acquire(self._device)      →  acquire(self._device, "main")
stream_manager.get_worker(self._device.id) →  get_worker(self._device.id, "main")
stream_manager.release(self._device.id)    →  release(self._device.id, "main")
```

En el doble del test se actualizó **solo `acquire`**. `get_worker` y `release`
quedaron aceptando un argumento, y el worker los llama con dos:

```
TypeError: <lambda>() takes 1 positional argument but 2 were given
  aurea_vms/core/analytics_engine.py:102, en stream_manager.release(...)
```

Corregido alineando el doble con las firmas reales de
`stream_manager.py:205,228,238` (las tres aceptan `kind: str = "main"`). El fallo
era del test, no del código de producción: el cambio de `7f6d5ff` es correcto y
está bien justificado (la analítica nunca debe consumir el sub-stream, porque las
coordenadas de ROI/línea se configuran sobre el flujo principal).

### Línea base final, en verde

| Chequeo | Resultado |
|---|---|
| `ruff check aurea_vms tests` | ✅ All checks passed |
| `ruff format --check` | ✅ 128 archivos ya formateados |
| `pytest -m "not integration" --cov` | ✅ **280 pasan**, cobertura **81.68%** (gate: 80%) |
| `pytest -m integration` | ✅ 1 pasa, en 0.62s |
| `python -m aurea_vms.main --smoke` | ✅ door_state, people_counting, line_crossing, face_detection |

Esta es la referencia contra la que se compara todo lo que siga.

**Cobertura por módulo, los huecos reales** (el gate del 80% es un promedio que
esconde módulos enteros sin ejercitar):

| Módulo | Cobertura |
|---|---|
| `core/desktop_notify.py` | 29% |
| `core/ptz_control.py` | 29% |
| `core/logging_setup.py` | 37% |
| `core/analytics/object_detector_backend.py` | 40% |
| `core/device_manager.py` | 44% |
| `core/analytics/face_detection_analyzer.py` | 68% |
| `core/stream_manager.py` | 70% |
| `core/analytics_engine.py` | 75% |

---

## Informe de validación del backend

Todo verificado contra el código, con archivo:línea. Fundamenta el plan por fases
que sigue.

### Bloqueantes para la demo

- **B1 — SQLite sin WAL ni `busy_timeout`.** `create_engine` solo pasa
  `check_same_thread=False`; el único PRAGMA es `foreign_keys=ON`
  (`models/db.py:21-29,46-52`). Escriben concurrentemente cuatro tipos de hilo:
  `StreamWorker` (estado online/offline, `stream_manager.py:138`), `AlarmEngine`
  desde el hilo de analítica (`alarm_engine.py:97`), `ClipWriter`
  (`media_store.py:52`) y `RetentionWorker` (`retention.py:82`). En modo
  rollback-journal y sin `busy_timeout`, una escritura concurrente falla con
  `OperationalError: database is locked`.
- **B2 — `alarm_engine._trigger` no tiene `try/except`.** Es el único escritor de
  DB del proyecto sin protección defensiva (`alarm_engine.py:97-104,127`), contra
  el patrón que sí siguen `stream_manager.py:140`, `clip_recorder.py:111` y
  `retention.py:120`. Si B1 se manifiesta, **se pierde la alarma** y la excepción
  sube cruda por un slot de Qt desde un hilo de analítica.
- **B3 — `QSystemTrayIcon` creado desde un hilo de analítica.** `AlarmEngine` no
  es `QObject`, así que `event_bus.detection.connect(self._on_detection)`
  (`alarm_engine.py:38`) no marshalea nada: `_on_detection` corre en el hilo del
  `AnalyticsWorker` y desde ahí llega a `desktop_notify.notify()`
  (`alarm_engine.py:126-132`), que construye un widget de Qt
  (`desktop_notify.py:16-32`). Encima `_tray_icon` es un global de módulo con
  doble-check sin lock (`desktop_notify.py:13,21-22`).
- **B4 — Un clip que no se pudo escribir se registra igual como evidencia.**
  `_write_mp4` nunca chequea `writer.isOpened()` (`clip_recorder.py:133-140`).
  `cv2.VideoWriter` no lanza excepción ante códec ausente o disco lleno: devuelve
  un writer cerrado, los `write()` no hacen nada, y `media_store.register()`
  inserta igual la fila en `media_assets` (`clip_recorder.py:142-151`). Queda un
  incidente con un `.mp4` vacío o corrupto y sin un solo log de error. Lo mismo
  con `cv2.imwrite` en `save_snapshot`, cuyo retorno tampoco se mira.
- **B5 — Reconexión RTSP sin backoff ni watchdog de congelado.**
  `RECONNECT_DELAY_S = 3.0` fijo para siempre (`stream_manager.py:39,93,117`).
  `is_stale()` existe (`stream_manager.py:171-177`) pero solo lo consume la UI: un
  stream que devuelve `ok=True` repitiendo el último frame decodificado no se
  reconecta nunca.

### Deuda de datos

- **D1 — Migraciones ad-hoc sin versionado.** `create_all` no altera tablas
  existentes; el gap se tapa con `_ADHOC_COLUMNS` + `ALTER TABLE ADD COLUMN`
  (`models/db.py:66,77-131`). No hay tabla de versión de esquema. Agregar una
  columna al modelo y olvidarla en el dict deja la DB de campo rota en silencio.
- **D2 — Migraciones de datos en el camino caliente de arranque.** Dos
  `UPDATE ... SET analyzer_name='door_state'` corren en **cada** `init_db()`,
  para siempre (`models/db.py:123-130`, agregados en `7f6d5ff`).
- **D3 — `AlarmRule` usa la API legacy `Column`/`Any`** mientras los otros 7
  modelos usan `Mapped`/`mapped_column` (`models/alarm_rule.py:19-40`). No es
  cosmético: con `Mapped[str]` SQLAlchemy infiere `nullable=False`; con
  `Column(String(60))` el default es **nullable=True**. Hoy `analyzer_name`,
  `min_confidence`, `cooldown_seconds`, `severity`, `actions` y `enabled` son
  nullables a nivel de esquema.
- **D4 — Falta unique en `analytics_configs(device_id, analyzer_name)`.**
  `upsert_analytics_config` y `get_analytics_config_for` usan `.one_or_none()`
  asumiendo una unicidad que el esquema no garantiza
  (`repository.py:177-187,190-211`). Dos filas duplicadas ⇒ `MultipleResultsFound`
  que rompe el arranque de analíticas (`main.py:69-82`).
- **D5 — Índices.** Falta uno en `alarm_events.status`, que
  `count_pending_alarm_events()` filtra periódicamente desde el dashboard
  (`repository.py:299-307`); `device_id` tiene índice propio **además** del
  compuesto `ix_alarm_events_device_ts` que ya lo cubre (`alarm_event.py:20,30`);
  falta unique en `zones(site_id, name)` (`zone.py:19-20`).
- **D6 — `setattr` sin whitelist** en los seis `update_*` de `repository.py`: un
  typo en un kwarg crea un atributo Python no mapeado que se pierde en silencio al
  commitear ("guardé y no pasó nada").

### Seguridad

- **S1 — Credenciales de cámara en texto plano** en `devices.password`
  (`models/device.py:24-25`, usadas en `device_manager.py:49-64`). Ya marcado 🔴
  en el ROADMAP como bloqueante pre-cliente.
- **S2 — PBKDF2 con 260.000 iteraciones** (`core/auth.py:18,26-27`), por debajo
  del mínimo OWASP actual (≥600.000). El salt de 16 bytes por usuario sí es
  correcto. Subir la constante sola no re-hashea nada: hace falta rehash-on-login.
- **S3 — Sin lockout ni rate-limit de login** (`core/auth.py:52-69`).

### Rendimiento

- **P1 — Una sesión ONNX por analizador, no por proceso.** `PeopleCountingAnalyzer`
  (`people_counting_analyzer.py:62`) y `LineCrossingAnalyzer`
  (`line_crossing_analyzer.py:55`) instancian cada uno su `YoloxDetector`, que crea
  su propia `InferenceSession` del modelo de 20 MB
  (`object_detector_backend.py:271-287`). Con N cámaras × 2 analíticas son 2N
  sesiones en RAM. Mitigado a medias con `intra_op_num_threads=2`.

### Tests y acoplamiento UI↔backend

- **T1 — El único test de integración no corre inferencia.** Construye los 4
  analizadores y llama `close()`, nunca `process_frame` con un frame real
  (`tests/test_registry.py:57-73`) — de ahí que tarde 0.62s. Una regresión en
  `_preprocess`, `_decode_outputs`, `_nms` o en YuNet **no la detecta nadie**: los
  unitarios mockean el modelo y el de integración no lo ejercita. Es el mayor
  riesgo silencioso de regresión del proyecto.
- **T2 — Lógica de dominio dentro de un widget, en el hilo de la GUI.**
  `ui/widgets/face_gallery.py:59-145` implementa firma de rostro, firma geométrica
  normalizada por distancia interpupilar, comparación combinada, dedup por umbral y
  reinicio diario. `_on_detection` (`face_gallery.py:244-300`) hace ese trabajo de
  CPU **en el hilo principal de Qt**, por cara y por frame — y
  `analytics_engine.py:44` documenta el caso de facial "en modo forense a 25fps".
  Además vive en `ui/`, o sea fuera del scope de cobertura, y no tiene un solo test.
- **T3 — Agregaciones en Python que deberían ser `COUNT` en SQL.**
  `event_dashboard.refresh()` trae 200 eventos + todos los devices para contar
  críticos y pendientes (`ui/modules/event_dashboard.py:123-134`);
  `dashboard_panel.refresh()` trae todos los devices solo para contarlos por
  `status` (`ui/widgets/dashboard_panel.py:154-171`). Ya está en el ROADMAP.
- **T4 — `device_manager.refresh_device_status` es código muerto**: cero llamadas
  en todo el repo, solo su definición (`device_manager.py:153-169`, verificado con
  grep). Y la I/O real de `device_manager` no tiene ningún test.
- **T5 — `build-windows.yml` solo corre por tag o a mano**
  (`.github/workflows/build-windows.yml:7-9`). Una regresión de empaquetado puede
  vivir en `main` hasta el próximo release — incluido el WSDL de ONVIF, que el spec
  empaqueta condicionalmente con `if _wsdl_dir.exists()` y sin fail-fast
  (`aurea_vms.spec:30-33`).
- **T6 — El ORM se filtra hasta la UI**: `ui/dialogs/device_dialog.py:18,129-135`
  importa y captura `sqlalchemy.exc.IntegrityError` porque `repository` no expone
  una excepción de dominio.

### Documentación vs realidad

El commit `1d78e77` sacó MediaPipe y puso YuNet + YOLOX-Tiny (ONNX), pero **ningún
documento se actualizó**. Quedaron desalineados `README.md:16,35,78`,
`docs/ARQUITECTURA.md:43,54-62` (EfficientDet-Lite2, BlazeFace, y el ADR
"MediaPipe se eligió sobre ultralytics"), `docs/ROADMAP.md:20-22,47,49`,
`docs/build_windows.md:14,37` y `.github/workflows/ci.yml:39,42-45,54` — el CI
todavía instala `libgles2` para una librería que ya no existe y rotula el paso de
integración como "modelos .tflite reales". `data/models/` solo contiene `.onnx`.

Dos ítems del ROADMAP **ya están hechos** y siguen listados como pendientes: el
data-dir bajo `sys.frozen` (`config/settings.py:24-33`, con test en
`tests/test_packaging_paths.py`) y el spec de PyInstaller onedir con smoke.

Y hay una desalineación de producto: README y ARQUITECTURA dicen que las 4
analíticas son movimiento / conteo / cruce / facial, pero el registry expone
`door_state`, `people_counting`, `line_crossing`, `face_detection`
(`registry.py:14-21`). `motion_detection` quedó solo como compatibilidad legacy.

### Observación nueva, salida de correr el smoke

Con `AUREA_DATA_DIR` apuntando fuera del repo, el smoke **bajó los dos modelos de
internet** en vez de tomarlos de `data/models/`. La causa:
`resources.bundled_path("models/x")` resuelve a `PROJECT_ROOT/models/x` en
desarrollo (`config/resources.py:24-26`), pero los modelos viven en
`PROJECT_ROOT/data/models/`. O sea el paso 2 de `ensure_model` (copiar del bundle)
**nunca acierta en dev**, y se cae al paso 3, que es la descarga.

En desarrollo normal no se nota, porque sin `AUREA_DATA_DIR` el data-dir *es*
`PROJECT_ROOT/data` y el paso 1 encuentra los modelos. Pero contradice la promesa
explícita del docstring de `model_assets.py:4-5` ("la demo puede correr SIN
internet, así que el download es el último recurso"): una notebook de demo con el
data-dir redirigido y sin internet no arrancaría las analíticas. A evaluar en la
Fase 1 o 6.

### Lo que está bien y no hay que tocar

Para que el informe no quede desbalanceado: la arquitectura de capas se respeta
(`core`/`models` no conocen widgets), todo hilo→UI pasa por el `EventBus` con
`QueuedConnection` en los 11 consumidores, el ref-counting del `StreamManager` está
protegido con `RLock` y probado con 8 hilos reales
(`tests/test_stream_manager.py::TestConcurrencia`), el apagado ordenado está bien
razonado (`main.py:100-109`: el post-buffer del clip se espera **antes** de cortar
streams), `get_session()` hace commit/rollback/close correctamente
(`db.py:169-182`), los tests existentes son de comportamiento real y no humo, y
toda la I/O de red disparada desde la UI pasa por `FunctionWorker` — no hay ni un
ONVIF llamado directo desde un slot.

---

## Plan acordado

11 fases, cada una un commit atómico hecho a mano por Daniel. Orden:

| Fase | Qué | Estado |
|---|---|---|
| 0 | Entorno reproducible, bitácora y línea base | ✅ hecha |
| 1 | Documentación y CI alineados con el stack ONNX | ✅ hecha |
| 2 | SQLite bajo concurrencia real (B1 + B2) | pendiente |
| 3 | La notificación de escritorio, en el hilo de Qt (B3) | pendiente |
| 4 | Evidencia que no miente (B4) | pendiente |
| 5 | Streams que se recuperan solos (B5) | pendiente |
| 6 | Red de tests contra regresiones de inferencia (T1 + T4) | pendiente |
| 7 | Lógica de dominio fuera de los widgets (T2 + T3 + T6) | pendiente |
| 8 | Alembic y versionado de esquema (D1 + D2) | pendiente |
| 9 | Constraints e índices que faltaban (D3-D6) | pendiente |
| 10 | Seguridad pre-cliente (S1-S3) | pendiente |
| 11 | Una sola sesión ONNX por modelo (P1) | pendiente |

Alembic (8) va antes que los constraints (9) a propósito: SQLite no permite
agregar `UNIQUE` ni cambiar `nullable` con `ALTER TABLE`, hace falta el ciclo
copiar-tabla que `batch_alter_table` resuelve.


---

## Fase 1 — Documentación y CI alineados con el stack ONNX

Cero cambio funcional: solo documentación, comentarios, docstrings y un trigger
de CI. Lint y tests en verde, misma cobertura que la línea base (81.68%).

### Qué se corrigió

- **`README.md`**: las 4 analíticas reales (estado de puerta en lugar de
  movimiento), stack ONNX Runtime con YOLOX-Tiny y YuNet en vez de "MediaPipe
  Tasks (modelos .tflite)", y la nota del CI ya no atribuye `libgles2` a
  MediaPipe.
- **`docs/ARQUITECTURA.md`**: sección de analíticas reescrita contra el código —
  YOLOX-Tiny con letterbox 416×416, relleno gris 114 arriba-izquierda,
  decodificación de las 3 cabezas por stride 8/16/32 y NMS class-agnostic
  (replica el demo oficial de ONNXRuntime de YOLOX); YuNet con sus 5 landmarks y
  los tres filtros geométricos; `door_state` con histéresis y solo transiciones.
  Se aclara que `motion_detection` sigue construible desde el registry por
  compatibilidad pero la UI ya no lo ofrece.
- **La nota de licencias se reescribió, no se borró.** El ADR viejo decía
  "MediaPipe se eligió sobre ultralytics/YOLO para evitar la AGPL-3.0", lo cual
  hoy se lee como contradicción porque el stack volvió a la familia YOLO. La
  versión correcta: YOLOX (Megvii) y YuNet (OpenCV Zoo) son **Apache 2.0**; la
  prohibición sigue siendo sobre **`ultralytics`** (AGPL-3.0), no sobre YOLO.
- **Dos errores del doc que no tenían que ver con MediaPipe**: decía "7 tablas" y
  son 8 (faltaba `zones`), y describía "borrar sitio → cámaras a 'Sin sitio'"
  cuando la jerarquía real es Sitio → Zona → Cámara y el borrado deja las cámaras
  "Sin zona".
- **`docs/ROADMAP.md` reescrito**: se movieron a una sección "Hecho" los cinco
  ítems ya resueltos (data-dir frozen, spec onedir + smoke, assets de marca, N+1
  de Alarmas, design tokens), se reescribieron los que nombraban EfficientDet /
  MediaPipe, y se incorporaron los hallazgos de esta validación con su
  archivo:línea.
- **`docs/build_windows.md`**: el VC++ Redistributable lo piden onnxruntime/opencv,
  y el smoke valida las DLL de onnxruntime (por eso el `collect_all`).
- **`.github/workflows/ci.yml`**: el paso de integración se llamaba "modelos
  .tflite reales" y el comentario de versión de Python justificaba el 3.12 con
  los wheels de mediapipe.
- **`.github/workflows/build-windows.yml`**: sumado un trigger de PR acotado a
  `aurea_vms.spec`, `pyproject.toml`, `requirements*.txt` y el propio workflow
  (T5). Una regresión de empaquetado no la ve ningún otro workflow, porque el
  spec resuelve DLLs nativas y el WSDL de ONVIF recién en runtime.
- **Docstrings del código**, que son los que más engañan a quien va a tocarlo:
  `core/analytics/model_assets.py`, `core/analytics/base.py`,
  `core/analytics_engine.py`, `main.py`, `config/resources.py`.
- **`tests/test_packaging_paths.py`**: los fixtures usaban nombres `m.tflite`.

### Lo que NO se tocó, a propósito

Los "MediaPipe" que quedan en `object_detector_backend.py`,
`face_detection_analyzer.py`, `pyproject.toml` y `aurea_vms.spec` son el *por qué*
de cada decisión ("reemplaza a EfficientDet-Lite2 porque…", "YuNet espera BGR, a
diferencia de MediaPipe"). La convención del proyecto es justamente que los
docstrings expliquen decisiones y trade-offs: borrar ese contexto empeoraría la
documentación, no la mejoraría.

### Una decisión que conviene registrar: `libgles2` se queda

El plan era sacar `libgles2` del CI, porque entró por MediaPipe
(`libmediapipe.so` linkeaba contra `libGLESv2.so.2`). Se verificó con `objdump`
que **ninguna** lib de onnxruntime, cv2 ni PySide6 declara `NEEDED` contra GLES.

Pero Qt puede cargarla por `dlopen` según el build, y esta máquina **ya tiene
`libGLESv2.so.2` instalada**, así que la corrida local en verde no prueba nada
sobre un runner limpio. Sacarla habría sido apostar el verde del CI que se acaba
de recuperar, en una fase cuyo objetivo era riesgo cero.

Decisión: se conserva el paquete y se corrige el comentario para que diga la
verdad (entró por MediaPipe, hoy se queda solo por el posible `dlopen` de Qt).
Queda como ítem verificable en el ROADMAP, a confirmar en un run real de Actions.

### Verificación

| Chequeo | Resultado |
|---|---|
| `ruff check` | ✅ |
| `ruff format --check` | ✅ 128 archivos |
| `pytest -m "not integration" --cov` | ✅ 280 pasan, 81.68% |
| `pytest -m integration` | ✅ |

Búsqueda final de rastros: `README.md`, `docs/` y los workflows quedaron sin
menciones de `.tflite`, EfficientDet ni BlazeFace.
