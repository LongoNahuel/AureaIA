# Sesión 2026-09-23 — Auditoría de lo hecho el 22 y build de Windows

Continuación de `sesiones/2026-09-22.md`, donde se cerró el plan de 11 fases.
Lo único que quedó pendiente fue **correr `build-windows.yml`**: las migraciones,
`cryptography` y sus binarios nativos entran al bundle por primera vez, y el
smoke del `.exe` se suponía la única prueba real de que llegaron.

Participantes: Daniel + Claude (Opus 5.5).

Antes de lanzar el build se re-auditó **el código tal como quedó ayer**, no la
bitácora. La auditoría se hizo con tres revisiones en paralelo:

- DB/Alembic;
- seguridad, sesión ONNX y empaquetado;
- hilos y workers.

Los hallazgos críticos se confirmaron leyendo el código a mano. La conclusión
que ordena el día es que **ese smoke no probaba lo que decía probar**: el build
tenía que esperar.

---

## Fase 0 — Línea base y auditoría

### Línea base

| Chequeo | Resultado |
|---|---|
| `pytest -m "not integration"` | ✅ **547 pasan** (igual que el cierre del 22) |
| `git status` | limpio, `main` = `origin/main` = `2b7fd6d` |

### Decisiones tomadas antes de codear

| Tema | Decisión |
|---|---|
| Build de Windows | Primero se arregla el smoke y después se corre el build. Lo dispara Dani desde la UI de GitHub, porque en esta máquina no hay `gh`. |
| Alcance backend | Entran los cuatro bloques: migraciones, clave de cifrado, prefs/retención + lockout, hilos/workers. |
| Hallazgos de UI | Se documentan para Nahuel en el ROADMAP, sin tocar la UI. |
| 0004 activa reglas inertes | Se corrige **en el lugar**, porque no hay bases de cliente migradas. En la de dev no afectó a nada: se comparó contra `.bak-pre-0004` y las cuatro reglas ya tenían `analyzer_name` y `enabled`. |
| Clave de cifrado perdida | La app arranca **degradada y avisa**: no crea una clave nueva, no conecta con contraseña vacía, y guardar una contraseña da error hasta que se restaure la clave o se confirme regenerarla. |
| Backups antes de migrar | Se conservan los últimos 3. |

### Lo más grave: el smoke del `.exe` no prueba el bundle

Los dos smokes del workflow comparten `AUREA_DATA_DIR`
(`build-windows.yml:31`, a nivel de job). El smoke previo al build deja la base
en head y copia los `.onnx` a `smoke-data/models`. Cuando arranca el `.exe`:

- `migrar` corta en `revision == cabeza` (`models/db.py:173`), así que
  **`env.py` nunca corre congelado** y no se ejecuta ningún `upgrade()` ni
  `batch_alter_table`.
- `ensure_model` encuentra los modelos ya copiados y **nunca toca** los datos
  `("data/models", "models")` del spec.

A eso se suman tres cosas que el smoke directamente no mira (`main.py:112-143`):

- **Fernet:** solo se ejercita el import. Ni la creación de la clave con
  `O_EXCL` en Windows, ni un cifrado real.
- **ONVIF:** ni `onvif`, ni `zeep`, ni el WSDL. Además el spec omite el WSDL
  **sin error** si no lo encuentra (`aurea_vms.spec:40-41`).
- **El filtro `paths` del trigger no cubre `aurea_vms/migrations/**`**: una
  revisión que rompa el bundle no dispara el build del PR.

Un build verde hoy habría dado una falsa sensación de cierre.

### Hallazgos backend (se arreglan hoy)

**ALTA**

- **Una migración que falla a mitad deja la app sin arrancar para siempre.**
  - SQLite tiene `transactional_ddl = False` en Alembic, y pysqlite en modo
    legado solo emite `BEGIN` antes del primer DML. Por eso el
    `CREATE TABLE _alembic_tmp_devices` del batch de 0005 (y el de 0003) se
    confirma solo.
  - Un disco lleno o un corte de luz durante la copia deja la tabla temporal,
    y cada arranque siguiente muere con `table _alembic_tmp_devices already
    exists`. Se reprodujo en memoria.
  - 0004 zafa por casualidad: arranca con un `UPDATE`. → Fase 2.
- **No existe ningún backup automático antes de migrar.** Los `.bak-pre-*` de
  ayer fueron manuales. Hay pasos que no se pueden deshacer: 0004 deduplica y
  0005 reescribe contraseñas. → Fase 2.
- **`camera_key` se regenera en silencio** (`core/credential_store.py:54-65`)
  aunque la base tenga filas `enc1:`.
  - Pasa al restaurar solo el `.sqlite3`, con un perfil nuevo o con un
    antivirus que ponga el archivo en cuarentena.
  - Las contraseñas quedan irrecuperables y los StreamWorker reintentan con
    contraseña vacía contra cámaras que bloquean la IP tras N fallos.
  - Encima, el downgrade de 0005 escribe `""` sobre todo si la clave no
    corresponde. → Fase 3.
- **La retención poda con los defaults si `preferences.json` está ilegible**
  (`core/app_prefs.py:22-35`).
  - `_write` trunca antes de escribir (no es atómico), y `_read` vuelve a los
    defaults (7 días / 5 GB) ante un `JSONDecodeError`.
  - Una pasada de retención que coincide con un guardado de preferencias, o un
    corte de luz a mitad de la escritura, **borra evidencia** de un sitio
    configurado a 90 días. → Fase 4.
- **Si el reloj va hacia atrás, el lockout no tiene tope** (`auth.py:108-111`).
  Con la pila CMOS muerta el equipo arranca en 2000, y `locked_until - now`
  pasan a ser años. Contradice el "nunca permanente" del modelo. → Fase 4.

**MEDIA**

- **Tras el primer bloqueo, cada fallo vuelve a bloquear**: `failed_attempts`
  no se resetea cuando el bloqueo vence (`auth.py:115-120`). → Fase 4.
- **Enumeración de usuarios por tiempo**: si el usuario no existe se devuelve
  sin correr PBKDF2 (≈0 ms contra ≈0,5 s). → Fase 4.
- **La clave se escribe sin atomicidad, sin `fsync` y sin lock.** Un corte deja
  un archivo de 0 bytes, `Fernet(b"")` lanza, y `cifrar` no lo atrapa: no se
  puede volver a guardar ninguna cámara. → Fase 3.
- **FKs activas durante un batch borran los hijos en cascada.** Se verificó:
  2 eventos → 0. Hoy no pasa solo porque `env.py` arma su propio engine sin el
  listener de pragmas, y nada fija esa invariante. → Fase 2.
- **No hay lock entre procesos alrededor de `migrar`.** Un doble clic el día
  del upgrade hace que la segunda instancia muera con "database is locked" o
  re-ejecute revisiones. → Fase 2.
- **`init_db` publica `_engine` antes de migrar**: si la migración falla, las
  globales quedan puestas contra una base sin migrar (`models/db.py:126-141`).
  → Fase 2.
- **0004 activa reglas inertes**: el saneo NULL→`door_state`/`enabled=1` hace
  que una regla que nunca matcheaba pase a disparar (`0004_constraints.py:45,52`).
  → Fase 2.
- **Hilos muertos registrados como vivos.** `StreamWorker.run()` no tiene `try`
  global y `process_frame` no está protegido. Un `cv2.error` inesperado mata el
  hilo, y `acquire()` sigue devolviendo el cadáver. No hay
  `threading.excepthook`: en el `.exe` sin consola no queda traza. → Fase 5.
- **La sesión ONNX y el StreamWorker se fugan si `acquire()` falla**, porque
  está antes del `try` (`analytics_engine.py:56`). Lo mismo si falla la
  construcción del analizador después de pedir la sesión
  (`line_crossing_analyzer.py:57-58`). → Fase 5.
- **Queda un mp4 huérfano si falla el indexado.** `clip_recorder.py:230-246`
  solo captura `MediaWriteError`, así que un `IntegrityError` (se borró la
  cámara mientras grababa) deja el archivo fuera de `media_assets`, y la
  retención no lo ve nunca. → Fase 5.
- **`alarm_engine._on_detection` corre en el hilo de la GUI, no en el de
  analítica.**
  - Se verificó con PySide6 6.11: el EventBus vive en el hilo principal, así
    que Qt encola el callback. Los docstrings dicen lo contrario.
  - Consecuencias: una query, un `imwrite` y dos INSERT por disparo en el hilo
    de la GUI; el snapshot sale del frame del momento de procesar, no del de
    detectar; y **no mira `_active`**, así que eventos ya encolados se procesan
    después de `stop()`.
  - Irónicamente, esto también significa que el `notify()` fuera de hilo que
    se arregló ayer nunca llegó a ocurrir con esta versión de PySide. El guard
    sigue valiendo como defensa.
  - Mudarlo de hilo se decide con números. → Fase 5.
- **El watchdog de congelado compara bytes exactos** de una grilla de ~2000
  píxeles (`stream_manager.py:82-90`). Una cámara con "smart codec"
  (H.264+/H.265+) y 3DNR fuerte en un pasillo quieto de noche puede entregar
  frames idénticos más de 15 s: se corta cada ~18 s y el estado alterna
  offline/online en la base. → Fase 6, midiendo primero con el rig.

**BAJA** (entran en la fase del módulo que tocan):

- Ruta de datos con `%`: el `ConfigParser` de Alembic lanza (`db.py:146-147`).
- `descifrar` loguea un error por fila y por carga.
- Hash corrupto → `ValueError` en vez de `False`, e iteraciones sin tope.
- `StreamWorker._report_status` escribe en la base después de `stop()`.

### Hallazgos de UI (para Nahuel, documentados en el ROADMAP)

- **ALTA — cerrar una pestaña con un `FunctionWorker` en curso aborta el proceso.**
  Se reprodujo: `QThread: Destroyed while thread is still running` + core dump.
  El worker tiene como padre al widget, y `main_window.py:296-301` hace
  `deleteLater()` sin esperar. Aparece en:
  - `device_management.py:315-325`
  - `ptz_control_panel.py:88-95`
  - `onvif_discovery_dialog.py:62-66`
  - `analytics_config_dialog_base.py:163-168`
- **MEDIA-ALTA — la `MainWindow` vieja sigue viva tras cerrar sesión.** Los
  lambdas que capturan `self` (`main_window.py:177-179`,
  `live_view.py:204,207,234`) la mantienen suscrita al `event_bus`. Con N
  cierres de sesión, cada alarma suena N+1 veces y los tiles ocultos siguen
  decodificando.
- **MEDIA — `DuplicateError` sin capturar** en sitios, zonas y usuarios
  (`sites_zones_module.py:263,269,287,293`, `user_management_module.py:195`).
  Con los UNIQUE nuevos, una zona repetida no se guarda y no avisa nada.
- **BAJA:**
  - `video_tile.py:329-336` lee `preferences.json` del disco dos veces por
    frame y por tile.
  - `face_gallery.py:179-196` recorta el frame actual con el bbox de otro
    frame.
  - El primer login con un hash legado corre ~860k iteraciones de PBKDF2 en el
    hilo de Qt.

### Lo que está bien hecho (y conviene no tocar)

- **Streams:**
  - Frame y historial bajo lock, con copias en las lecturas.
  - El backoff se interrumpe en el acto.
  - El contador solo se resetea si la conexión entregó frames.
- **Media y retención:**
  - El orden de apagado espera los clips antes de cortar streams.
  - La media se indexa recién después de `release()`/`imwrite`.
  - La retención borra primero el archivo y después la fila (se autorrepara).
  - `save_snapshot` y `_discard` nunca lanzan.
- **Base y migraciones:**
  - El corte temprano de `migrar` en head es correcto.
  - 0004 sanea en el orden correcto.
  - El cifrado de 0005 es reentrante.
  - Ninguna revisión importa modelos.
  - `get_session` siempre cierra.
- **ONNX:** la caché de sesiones tiene lock, y `InferenceSession.run`
  concurrente es seguro con las opciones que usamos.

### Deuda que se documenta y no se hace hoy

- **Portabilidad de las migraciones a PostgreSQL:**
  - 0002, 0003 y `ayudas.py` usan `PRAGMA` y `sqlite_master`.
  - 0002 y 0004 comparan enteros contra booleanos.
  - `db.py` tiene `sqlite:///` fijo en el código.

  La regla de "diseño portable" se cumple en los modelos, no en la cadena de
  migraciones.
- **La adopción de bases legadas crea tablas con el metadata vivo**: una
  `create_table` o un `index=True` futuros pueden romperla.
- **El `naming_convention` de `env.py` no hace nada**: `context.configure` lo
  ignora, y las bases adoptadas conservan constraints anónimas.
- **ACL de `camera_key` en Windows**: el 0600 no significa nada ahí; hereda la
  ACL de la carpeta, que bajo `%LOCALAPPDATA%` es aceptable.
- `_traducir` decide por el texto del mensaje de error; el `imencode` del JPEG
  corre bajo el lock del stream.

---

## Fase 0b — El esquema de Nahuel, de vuelta en Alembic

No estaba en el plan. Al pushear la Fase 0, el remoto tenía 5 commits nuevos
de Nahuel (`44ae65c`, `7487ef9`, `50950eb`, `cc04800`, `9834a87`, 22–23/09):
38 archivos y +1714 líneas, con sitios asignados sin zona, grabadores NVR/XVR
con canales y la vista inteligente. Nuestro commit era solo de docs, así que
el `git pull --rebase` pasó limpio. Lo que trajo dejó **`main` en rojo en el
CI**:

| Chequeo sobre `origin/main` | Resultado |
|---|---|
| `ruff format --check` | ❌ 15 archivos sin formatear (el job de lint del CI falla) |
| `pytest -m "not integration"` | ❌ **4 fallan**, 547 pasan, cobertura 89,05 % |

### La causa: el sistema ad-hoc volvió

`models/db.py` traía de vuelta `_ADHOC_COLUMNS` y `_apply_adhoc_migrations()`,
que hacen `ALTER TABLE ADD COLUMN` y varios `UPDATE` **en cada arranque**,
incluso con la base ya en head. El comentario lo delata: "Alembic reemplaza
esto cuando el esquema se estabilice". El código salió de una rama anterior
al 22/09.

La deriva real era chica. De las 9 columnas del dict, 7 ya estaban en la
baseline. Las nuevas son dos, `devices.assigned_site_id` (que en Python es
`Device.site_id`) y `devices.parent_device_id`, **sin sus índices** porque
ALTER TABLE no los crea: el mismo bug que se encontró el 22 con
`ix_devices_zone_id`. El test de deriva lo marcó: 2 columnas, 2 índices y
2 FKs.

Además traía tres pasos de datos:

- el backfill de zonas desde el `site_id` legado, que ya es la 0002 (y la
  0003 borra esa columna, así que ahí no hacía nada);
- el rename `motion_detection → door_state`, que también es la 0002;
- el agrupado de canales NVR/XVR sueltos bajo un padre, que sí es nuevo.

### Por qué se había reflotado: la adopción con metadata vivo

Nahuel llamaba al ad-hoc **antes** de `adoptar()` porque sin eso la adopción
de una base legada moría. `adoptar` crea los índices del metadata vivo, y el
de hoy declara `ix_devices_parent_device_id` sobre una columna que la tabla
legada no tiene: "no such column". Es exactamente la bomba que la auditoría
de esta mañana había dejado documentada (M3), y explotó con la primera
columna nueva después de la baseline.

### Lo que se hizo

- **`models/db.py` vuelve a ser idéntico al de `2b7fd6d`** (verificado con
  `diff`). Se van `_ADHOC_COLUMNS`, `_apply_adhoc_migrations`,
  `_backfill_zones_from_legacy_site_id` y `_group_legacy_recorder_channels`.
- **Revisión `0006_jerarquia_grabadores`**:
  - Agrega las dos columnas con FK nombrada (`SET NULL` / `CASCADE`) vía
    batch, y sus dos índices.
  - **Todo chequeado antes**, según la regla de `ayudas.py`: la base de
    Nahuel ya tiene las columnas por el ad-hoc, pero no los índices.
  - Incluye el agrupado de canales como paso de datos **de una sola vez**,
    con SQL portable: el original usaba `onvif_port IS ?` e `instr()`, que
    son de SQLite.
  - El downgrade saca todo sin perder cámaras.
- **`adoptar()` crea solo los índices cuyas columnas existen**. El resto lo
  crea la revisión que agrega la columna. Esto desactiva M3 para índices; el
  caso `create_table` sigue documentado.
- **`test_repository_kwargs`**: `site_id` volvió a ser un atributo válido de
  `Device` y los tests lo usaban como ejemplo de kwarg inválido. El tropiezo
  realista pasa a ser el inverso, `assigned_site_id`, que es el nombre de la
  columna en la base pero no el del atributo.
- **`ruff format`** sobre los 14 archivos de Nahuel que lo necesitaban (solo
  formato, cero cambios de lógica).
- **Guarda contra la regresión**: un test falla si `models/db.py` vuelve a
  contener `ALTER TABLE` o `_ADHOC`.

### Los tests discriminan

Se revirtió cada pieza a mano:

| Reversión | Resultado |
|---|---|
| `adoptar()` sin el filtro de columnas | 11 tests de adopción/idempotencia fallan |
| 0006 con `onvif_port = :x` (sin comparar NULL) | 5 de `TestCanalesSueltos` fallan |
| 0006 sin chequear que la columna exista | 2 de `TestBaseDelSistemaAdhoc` fallan ("duplicate column") |
| `db.py` de Nahuel con la 0006 presente | falla la guarda de ad-hoc |
| sin la 0006 (el estado de `origin/main`) | 13 fallan, incluida la deriva |

### Verificación

| Chequeo | Resultado |
|---|---|
| `ruff check` / `ruff format --check` | ✅ / ✅ |
| `pytest -m "not integration" --cov` | ✅ **562 pasan**, cobertura **90 %** |
| `pytest -m integration` | ✅ 10 pasan |
| Copia de la base de dev (0005) → `init_db` | ✅ queda en `0006_jerarquia_grabadores`, 4 cámaras intactas |

Cobertura por módulo de lo tocado: `models/db.py` 99 %, `adopcion.py` 97 %,
`repository.py` 97 %. El total bajó de 91,37 % (22/09) a 90 % por el código
nuevo de UI y analíticas de Nahuel, no por lo de acá.

### Lo que destapó: la base de dev tiene deriva desde antes de hoy

`compare_metadata` sobre la base de dev, **antes** de la 0006, marca cosas
que no son de hoy:

- **`devices.zone_id` tiene la FK sin `ON DELETE SET NULL`.** La columna se
  agregó con un ALTER de antes de ese arreglo, y los batch de 0003 y 0005 la
  copiaron tal cual. Resultado: borrar una zona con cámaras, con
  `foreign_keys=ON`, falla en esa base y no en una nueva. Es probablemente el
  síntoma que llevó al `delete_device` nuevo de Nahuel a borrar a mano
  ("FKs NO ACTION aunque el modelo declare CASCADE").
- Faltan los UNIQUE de `sites.name`, `users.username` y
  `media_assets.rel_path`.

El test de deriva solo mira bases nuevas, así que nunca lo vio. Es M4 con
evidencia: **las bases adoptadas no quedan con el esquema de los modelos**.
Va a la Fase 2 como una revisión de normalización.

### Archivos tocados

- `aurea_vms/models/db.py` — vuelve al de `2b7fd6d`
- `aurea_vms/migrations/versions/0006_jerarquia_grabadores.py` — nueva
- `aurea_vms/migrations/adopcion.py` — índices solo sobre columnas existentes
- `tests/test_migracion_0006.py` — nuevo, 10 tests
- `tests/test_db_migrations.py` — adopción con columnas posteriores a la baseline
- `tests/test_repository_kwargs.py` — `assigned_site_id` como kwarg inválido
- 14 archivos de `aurea_vms/core` y `aurea_vms/ui` — solo `ruff format`

---

## Fase 1 — Un smoke que prueba lo que dice probar

### Lo que había

El smoke vivía en `main.py::_smoke_test`: `init_db()`, los 4 analizadores
con un frame negro y una `QApplication`. Tenía tres agujeros:

- los dos smokes del workflow compartían el data-dir;
- no ejercitaba Fernet ni ONVIF;
- un modelo que faltara en el bundle se **descargaba** sin avisar, así que
  el smoke pasaba igual. Con el data-dir compartido, además, ni siquiera
  llegaba a buscarlo.

### Lo que hay

**`aurea_vms/smoke.py`** (nuevo; `main.py` solo delega). Imprime una línea
`smoke: <chequeo> OK` por chequeo:

| Chequeo | Qué ejercita de verdad |
|---|---|
| `migraciones` | La base quedó en la cabeza. Con base nueva dice "corrieron todas las revisiones": `env.py` y las 6 revisiones, leídas desde el bundle. |
| `cifrado` | Una cámara con contraseña ida y vuelta: crea `camera_key` (`os.open` con `O_EXCL`, que en Windows es otro código), pasa por el `TypeDecorator` y los binarios `_rust` de `cryptography`. Lee la columna **cruda** con SQL de texto, porque un `select` sobre la tabla pasa por el `TypeDecorator`, devuelve el valor descifrado y un cifrado roto pasaría igual. Borra la cámara al final. |
| `onvif` | `import onvif, zeep, lxml, wsdiscovery` y parseo de `devicemgmt.wsdl` con un transporte que **prohíbe la red**, exigiendo que exista el `DeviceBinding`. |
| `analizador <x>` | Los 4 analizadores con sus modelos reales, **con las descargas prohibidas**: un modelo que falte en el bundle es una falla, no un plan B. |

Si el data-dir no está vacío, imprime `smoke: AVISO ...`.

**`build-windows.yml`**:

- **Un data-dir vacío por smoke** (`smoke-pre` y `smoke-exe`).
- El paso del `.exe` corre en bash y **exige** "base nueva: corrieron todas
  las revisiones" y "SMOKE OK", y **falla si aparece un AVISO**.
- Hay una trampa que se evitó y quedó comentada: con `set -e`, bash
  **ignora** los comandos negados con `!`. Un `! grep -q AVISO` no habría
  hecho fallar nunca el paso; se comprobó en esta máquina y por eso va un
  `if ... exit 1`.
- `PYTHONDONTWRITEBYTECODE` y `PYTHONIOENCODING=utf-8`.
- El filtro `paths` del PR ahora incluye `aurea_vms/migrations/**`,
  `smoke.py`, `main.py`, `resources.py`, `credential_store.py` y
  `model_assets.py`.

**`aurea_vms.spec`**:

- **Fail-fast si no está el WSDL**. Antes era un `if exists()` mudo que
  compilaba igual. Cierra el ítem del ROADMAP.
- `aurea_vms.migrations.ayudas` en `hiddenimports`: solo lo importan las
  revisiones, así que el análisis estático no lo ve.
- Las revisiones se copian **archivo por archivo y sin `__pycache__`**.
  Copiar la carpeta entera se llevaba los `.pyc` de cualquier corrida
  previa: pasó en el primer build de hoy, por un `pytest` que corría en
  paralelo.

**`models/db.py::_config_de_alembic`**: escapa `%`. Se reprodujo que una ruta
como `C:/Users/ana%20/...` hacía que `set_main_option` levantara
`ValueError: invalid interpolation syntax`, o sea que la app no arrancaba
para ese usuario.

### Los tests discriminan

`tests/test_smoke.py` tiene 10 unitarios y 1 de integración. El de
integración corre `python -m aurea_vms.main --smoke` en un subproceso con
data-dir vacío, así que **el smoke completo corre en el CI de Linux en cada
push**, no solo en el build de 45 minutos.

Uno de los tests encontró un bug antes de existir el arreglo: zeep acepta
**sin quejarse** un `devicemgmt.wsdl` basura que no define nada. De ahí salió
el chequeo del `DeviceBinding`. Se revirtió cada pieza a mano:

| Reversión | Resultado |
|---|---|
| sin escapar `%` | falla `test_una_ruta_con_porcentaje_no_rompe_el_arranque` |
| sin chequear que la columna esté cifrada | falla `test_falla_si_la_contrasena_queda_en_claro` |
| sin chequear el `DeviceBinding` | falla `test_empaquetado_con_wsdl_incompleto_falla` |
| sin prohibir descargas | falla `test_un_modelo_ausente_falla_en_vez_de_bajarse` |

### Verificación

| Chequeo | Resultado |
|---|---|
| `ruff check` / `ruff format --check` | ✅ / ✅ |
| `pytest -m "not integration" --cov` | ✅ **572 pasan**, cobertura **89 %** |
| `pytest -m integration` | ✅ **11 pasan** (el smoke completo es el nuevo) |
| PyInstaller en Linux + smoke del ejecutable con data-dir vacío | ✅ todo `OK`; modelos "copiados desde `_internal/models`", WSDL desde `_internal/wsdl`, **0 descargas**; 11 fuentes de migraciones, 0 `.pyc`; 733 MB |

La cobertura baja de 90 % a 89 % porque `aurea_vms.smoke` pasó a medirse
(62 %). Lo que falta, `run()` y los analizadores, lo cubre el test de
integración, que no suma a la cobertura del CI: es el `--cov-append`
pendiente.

**No verificado**: el fail-fast del WSDL en el spec no se ejercitó con una
carpeta ausente de verdad. Habría que romper el `site-packages` del venv.

### Archivos tocados

- `aurea_vms/smoke.py` — nuevo
- `aurea_vms/main.py` — `_smoke_test` delega en `smoke.run()`
- `aurea_vms/models/db.py` — escape de `%` en la config de Alembic
- `.github/workflows/build-windows.yml` — data-dirs separados, aserciones, paths
- `aurea_vms.spec` — fail-fast del WSDL, `ayudas`, migraciones sin `__pycache__`
- `pyproject.toml` — `aurea_vms.smoke` en la cobertura
- `tests/test_smoke.py` — nuevo

---

# Cierre del día

| # | Commit | Qué | Tests | Cobertura |
|---|---|---|---|---|
| — | `2b7fd6d` | línea base al arrancar | 547 | 91,37 % |
| 0 | `0257e6e` | bitácora + auditoría del 22 | 547 | 91,37 % |
| — | `9834a87` | llegan 5 commits de Nahuel: **main en rojo** | 547 + 4 ❌ | 89,05 % |
| 0b | `2b9c6e2` | `ruff format` de lo que entró | — | — |
| 0b | `7e0ba23` | esquema de grabadores a Alembic (0006), se va el ad-hoc | 562 | 90 % |
| 1 | — | smoke que prueba el bundle | **572 + 11** | **89 %** |

Lo que encontramos y no estaba en el plan:

1. **El smoke del `.exe` no probaba el bundle.** Era la premisa del
   pendiente de ayer: el build habría salido verde sin probar migraciones,
   cifrado, ONVIF ni modelos.
2. **El sistema ad-hoc volvió a `main`** en commits de Nahuel, desde una rama
   anterior a Alembic, y dejó el CI en rojo.
3. **La bomba del metadata vivo en la adopción explotó** con la primera
   columna nueva después de la baseline. Fue el motivo por el que se
   reflotó el ad-hoc.
4. **La base de dev tiene `zone_id` sin `ON DELETE SET NULL`** desde la
   adopción: las bases adoptadas no quedan con el esquema de los modelos, y
   el test de deriva solo mira bases nuevas.
5. **zeep acepta un WSDL que no define nada.**
6. **`! grep` bajo `set -e` no falla nunca.**
7. **Una ruta con `%` rompía el arranque.**

## Arranque de la próxima

1. **Correr `build-windows.yml`**: Actions → "Build Windows" → *Run workflow*
   sobre `main`. Ya no es un smoke que pasa solo: el paso del `.exe` exige
   base nueva, cero AVISOS y "SMOKE OK". Si falla, el log dice qué chequeo
   cayó. Anotar acá cada línea `smoke:` y el tamaño del zip.
2. **Fase 2 — migraciones atómicas**: DDL transaccional real, FKs OFF en
   `env.py`, limpieza de `_alembic_tmp_*`, backup previo con rotación de 3,
   lock entre procesos, `init_db` sin globales a medias y la 0004 sin
   activar reglas inertes. **Suma**: una revisión que normalice las bases
   adoptadas (`zone_id` con `SET NULL`, UNIQUE faltantes) y un test de
   deriva **sobre una base adoptada**.
3. Fases 3 a 7 del plan, sin cambios: clave perdida en modo degradado,
   prefs atómicas + lockout, hilos que mueren a la vista, watchdog medido
   con el rig, cierre.
4. **Hablar con Nahuel**: toda columna va en una revisión de
   `aurea_vms/migrations/versions/`, y hay un test que lo fija. De paso, los
   ítems de "UI — para Nahuel" del ROADMAP, empezando por el crash al cerrar
   una pestaña.

---

# Parte 2 — Front y analíticas (Nahuel)

Participantes: Nahuel + Claude (Opus 5.5). Trabajo hecho en paralelo a la
Parte 1, sobre `9834a87`, e integrado encima de `8f4515b` con `git pull`. Solo
chocó el `ruff format` de `2b9c6e2` en 5 archivos (puro formato) y se resolvió
con la versión nueva ya formateada. **No se tocó el esquema de la base.**

Regla acordada y registrada: **toda columna nueva va en una revisión de
`aurea_vms/migrations/versions/`**, y se respeta la arquitectura del backend.

Hilo conductor: **las analíticas contaban cosas que no pasaban y la interfaz
mostraba números que no significaban nada**.

- Una regla de Cruce de Línea alarmaba con *ver* a una persona, no con que cruzara.
- El "máximo de personas" no disparaba nada.
- La galería de rostros recortaba otro cuadro.
- El chip de la puerta marcaba siempre 0.

## 1. Lógica de analíticas

### Tracker

- **Asociación global** por costo, en lugar de por orden de detección. Antes
  la primera caja se robaba el track y había cambios de identidad.
- **Distancia máxima proporcional al tamaño** del objeto. Con 80 px fijos, una
  persona cerca de una cámara 1080p a 2–5 fps perdía el track y no se contaba
  su cruce.
- **Predicción por velocidad** (media móvil).

### Cruce de Línea

- Cuenta cruces del **segmento dibujado**, no de la recta infinita.
- **Banda muerta** (10 % del alto de la caja): alguien parado sobre la línea
  ya no cuenta entrada/salida en loop.
- "Aplicar sentido" desactivado **dejaba de contar todo**. "Filtro mejorado"
  no hacía nada. Ahora cuenta en ambas direcciones y exige proporción de persona.

### Conteo de Personas

- **Aforo que alarma**: dispara una vez al alcanzarlo (flanco). Sin aforo, la
  presencia sigue alarmando (intrusión fuera de horario).
- **Mapa de calor real** (`core/analytics/heatmap.py`): grilla de densidad con
  vida media de 10 min, marcada en los pies. Antes eran los últimos 200 puntos
  (~8 s) y 800 elipses por cuadro.
- Ocupación por zona (`zones`) y pico (`peak`).

### Puerta

- **Compensación de brillo** (comparación contra la mediana) y adaptación lenta
  de la referencia con la puerta cerrada. Antes un cambio de luz la dejaba
  "abierta" para siempre.
- Métricas nuevas: `desde`, `aperturas`, `umbral` y `zona`. La caja es la de
  la zona que disparó.

### Worker de analítica

- `process_frame` protegido: una excepción se loguea (la 1.ª y después cada
  100) y el hilo sigue.
- `WorkerStats` con fps reales, latencia y errores
  (`analytics_engine.stats(config_id)`).

## 2. Rostros

- **El recorte sale del mismo cuadro de la detección.** Cierra el ítem BAJA
  del ROADMAP `face_gallery.py:179-196`.
- **Identidad con SFace** (OpenCV Zoo, Apache 2.0) + continuidad de track.
  Sin el modelo se vuelve a las firmas de antes.
  - Modelo versionado: `data/models/face_recognition_sface_2021dec.onnx`
    (**38,7 MB**). Entra al bundle por `("data/models", "models")`, pero **el
    smoke nuevo no lo carga todavía**.
- **Puntaje de calidad por toma** (frontalidad, nitidez, tamaño, confianza):
  queda la mejor toma, no la más grande.
- Medido: SFace tarda **~18 ms/cara** y solo corre cuando la toma mejora. Con
  imágenes de prueba de OpenCV, la similitud entre personas distintas fue
  **0,15** y la de la misma persona espejada **0,87** (umbral 0,40).
- **Registro único** (`ui/face_registry.py`): la galería, la tira y el visor
  dan el mismo ID para la misma persona. Guarda el historial de todas las
  tomas (12 por identidad).
- **Visor forense** (`ui/widgets/face_forensics.py`):
  - zoom de 25 a 1600 % y modo "píxeles reales";
  - vista Escena con la caja marcada;
  - realces **solo de visualización**;
  - ficha con desglose de calidad, distancia entre ojos y SHA-256;
  - exporta `rostro_original.png` sin pérdida, la escena, la escena marcada y
    `ficha.json` con los hashes.

## 3. Interfaz

- **Paneles de la Vista Inteligente**: frescura de datos, fps y latencia
  reales, estados con texto y tendencias de 10 min con tooltip.
- **Overlay**:
  - flechas de sentido con contador;
  - zonas de puerta teñidas por estado;
  - ocupación por zona;
  - marcador de rostro de esquinas;
  - se descartan las lecturas viejas.
- **Dashboard analítico** ("Video en vivo | Dashboard analítico"): una sección
  por analítica, con indicadores, gráfico y desglose por cámara. El par
  azul/naranja de entradas/salidas pasó el validador de paleta.
- Analizadores: el historial crecía **sin tope** (una entrada por cuadro).
  Ahora son intervalos fijos, y la columna Estado muestra la telemetría real.
- Tira "Rostros recientes" y autoselección del primer recuadro con cámara.

## Contratos compartidos que cambiaron (para Daniel)

Todos tienen valor por defecto, así que el código existente sigue funcionando:

- `DetectionEvent.triggers` / `AnalysisResult.triggers`: lo que dispara reglas
  cuando no coincide con lo que se dibuja (`None` = `detections`).
  `alarm_engine._on_detection` los usa si vienen.
- `FaceShot` (en `core/events.py`) viaja en `metrics["face_shots"]`.
- `CatalogUpdate.identity`, y `FaceCatalog.observe(embedding=, quality=,
  track_key=)` + `sightings()`.
- `AnalyticsEngine.stats(config_id) -> WorkerStats | None`.

**Se cruza con las fases pendientes de la Parte 1:**

- Fase 5 ("hilos muertos"): `AnalyticsWorker` ya protege `process_frame`.
  Falta lo de `StreamWorker`, `excepthook` y la fuga de `acquire()` antes del
  `try` (`analytics_engine.py`), que no se tocó.
- El hallazgo de que `alarm_engine` corre en el hilo de la GUI sigue en pie;
  el cambio de `triggers` no lo modifica.

## Verificación

| Chequeo | Resultado |
|---|---|
| `ruff check` / `ruff format --check` | ✅ / ✅ 173 archivos |
| `pytest` (tras integrar con `8f4515b`) | ✅ **636 pasan**, 0 fallan |
| Tests nuevos | `test_analytics_precision.py` (22), `test_face_identity.py` (15), `test_forensics_dashboard.py` (16) |
| App real | cámaras 1 y 2, las 4 analíticas, sin tracebacks |

No se midió cobertura.

## Observaciones de campo (cámara "Sistemas")

- Cámara cenital de gran angular: las caras miden **4–15 px entre ojos**
  (para identificar hacen falta ~40).
- Tiene confianza 0,2, muy permisiva; se recomienda 0,5.
- Inferencia medida: YOLOX 95–224 ms, YuNet ~180 ms. Los fps configurados
  (15–30) no se alcanzan; conviene 4–8.

## Arranque de la próxima (front)

1. **Módulo "Rostros" propio**, pedido de Nahuel al cierre y no empezado.
   - Va al **final** de `MODULES`, en Operación, con `LIVE_VIEW`.
   - Pantalla:
     - grilla de tarjetas por persona;
     - filtros por cámara, orden, calidad e ID;
     - detalle con forense, exportar y "ver en vivo";
     - gráfico de tomas.
   - La Vista Inteligente se queda con Personas, Cruce y Puertas, más una
     tarjeta que abre el módulo.
   - `face_registry` y `analytics_hub` se conectan al bus en `MainWindow`.
   - Tope de 120 personas por cámara, con presupuesto de memoria.
2. **Exportar en el visor forense debe exigir `Perm.EVIDENCE_EXPORT`**: hoy
   lo ve cualquier rol.
3. Ítems UI del ROADMAP, empezando por el **crash al cerrar una pestaña con
   un `FunctionWorker`**. Además:
   - `video_tile._draw_overlay` sigue llamando a
     `app_prefs.intelligent_branding_enabled()` dos veces por cuadro;
   - `_on_open_analytics_config_requested` abre `MODULES[2]` (Dispositivos,
     no Analizadores).
4. Rostros, historial del dashboard y contadores de cruce viven en memoria.
   Persistirlos requiere tablas: eso es backend y va por Alembic.

---

# Parte 3 — Rostros sin identidad y Monitor roto (Nahuel)

Participantes: Nahuel + Claude (Opus 5.5). Sigue sin commit. Para probar sin
cámaras se armaron **4 cámaras simuladas** con clips de casino de Nahuel
(`tools/demo/media/cam1..4.mp4`, ignorados por git, servidos por mediamtx),
dadas de alta como dispositivos #3 a #6.

## Rostros: fuera la identidad, queda la mejor captura

Pedido: sin "personas únicas", solo la mejor toma donde el rostro se vea bien.

- Se fue el reconocimiento facial (SFace) y el conteo con reinicio diario.
  **Se borró `core/face_catalog.py` y sus 39 tests** (Daniel, `73382cf`):
  solo servía para identidad y conteo, y ya nada lo usaba.
- Una captura = un paso de una cara por la cámara (un track), con su mejor toma.
- **Puerta de "rostro visible"** (`face_quality.is_face_visible`):
  confianza ≥ 0,80, frontal ≥ 0,6, ≥ 18 px entre ojos, nitidez ≥ 0,30.
  - Medido sobre los clips: las tomas con MEJOR puntaje eran falsos positivos
    (el centro de una ruleta y pantallas de tragamonedas, puntaje 0,92 con
    confianza de 0,52-0,64); las caras reales daban 0,88-0,95.
  - Resultado: 0 capturas en los clips sin caras, 8 caras reales y nítidas en
    cam4.
- Botón **Limpiar** en la sección Rostros del dashboard y en la tira
  "Rostros recientes".

## Puerta abierta → Monitor roto (golpes)

Pedido: zona de intrusión sobre el monitor de la máquina, que detecte el golpe
con manos, pies o patadas.

- **Pose con RTMPose-s** (OpenMMLab, Apache 2.0).
  - Motivo: YOLOX casi no detecta personas en cámaras cenitales (1 detección
    en 8 cuadros con dos jugadores).
  - Se estima la pose sobre el puesto: la pantalla agrandada ×2,2. No hace
    falta detectar a la persona.
  - Modelo versionado en `data/models/` (**22 MB**). RTMPose-t no detectaba
    las patadas.
- Reglas, calibradas sobre el clip "MONITOR ROTO 01":
  - **Patada**: un tobillo dentro de la pantalla, más otra articulación de la
    pierna o un tobillo con confianza ≥ 0,45. Las rodillas solas no cuentan:
    una jugadora con las piernas cruzadas dejaba las dos rodillas en el borde
    del rectángulo.
  - **Golpe con la mano**: la muñeca llega a ≥ 2,0 diagonales de pantalla por
    segundo. Las terminales son **táctiles**: en los toques normales la
    muñeca va a 0,03-0,11 (máximo 1,13). **El clip no trae un golpe real con
    la mano**: este umbral quedó calibrado contra el juego normal.
  - Un ataque es un incidente, y solo su inicio dispara la alarma.
- Validación:

  | Clip | Qué pasa | Resultado |
  |---|---|---|
  | cam3, zona de Nahuel | patadas a los 56-58 s | 1 incidente a los 57,1 s (5, 8 y 15 fps) |
  | cam3, monitor vecino | toques normales | 0 |
  | cam1, dos pantallas | 25 s manipulando la máquina | 0 |

  Costo: ~20 ms por pantalla y muestra.

## Para Daniel (backend)

- **Revisión `0007_monitor_roto`**: solo datos, sin cambios de esquema.
  - Renombra `door_state` → `monitor_tamper` en configs y reglas.
  - Conserva zonas y fps. Los parámetros de puerta pasan a `puerta_legado`,
    así que el downgrade los restaura exactos.
  - Las reglas quedan con clases vacías (cualquier golpe).
  - Tests en `test_migracion_0007.py`.
  - **Si tenés una 0007 en curso, hay que re-encadenar el `down_revision`.**
- `repository._normalize_analyzer_name`: `motion_detection` y `door_state`
  → `monitor_tamper`.
- `smoke.check_analizadores`: Monitor roto necesita una zona, igual que Cruce
  necesita la línea. Así el smoke **también carga el modelo de pose** desde el
  bundle.
- Tests tuyos ajustados:
  - `test_smoke.py` y `test_migracion_0006.py` comparaban contra `"0006_"`
    fijo; ahora contra la cabeza real de Alembic.
  - En `test_db_migrations.py`, la cadena 0002 → 0007 deja la config legada
    en `monitor_tamper`.
- `alarm_engine._on_detection` todavía tiene el caso especial de
  `door_state`. Ahora no hace nada; su test sigue pasando. Queda para tu
  limpieza.

## Verificación

| Chequeo | Resultado |
|---|---|
| `ruff check` / `format --check` | ✅ / ✅ |
| `pytest` | ✅ **616 pasan**, 0 fallan |
| `python -m aurea_vms.main --smoke` | ✅ SMOKE OK, con `monitor_tamper` y el modelo de pose |

# Parte 4 — Terminal limpia, marcas inteligentes y Detección de incidentes (Nahuel)

Participantes: Nahuel + Claude (Opus 5.5). Sigue sin commit.

## Errores de H.264 en la terminal

Síntoma: cientos de `[h264] error while decoding MB`, `co located POCs
unavailable`, `mmco: unref short failure` y `Missing reference picture`.

- **Causa: el rig de demo, no el VMS.** Por cada lector, mediamtx tiene una
  cola de 512 paquetes RTP (~740 KB). Los clips van a 7-12 Mbps, con I-frames
  de hasta 812 KB (~570 paquetes).
  - Un solo I-frame casi llena la cola. Si el lector del VMS se demora un
    instante, mediamtx descarta cuadros ("reader is too slow") y al decoder le
    faltan referencias.
  - Arreglo en `tools/demo/mediamtx.yml`: `writeQueueSize: 4096` (~5 s de
    video). Antes, `rtspTransports: [tcp]`, porque por UDP se perdían paquetes.
- Medido sin UI, con las 5 analíticas habilitadas de la base:

  | | Cuadros descartados | Errores H.264 |
  |---|---|---|
  | Cola de 512, ventanas de 36 s (7 corridas) | 129 a 690 | 7 a 19 |
  | Cola de 4096, 37 s y 60 s | 0 | 0 |

  Quedan unas pocas líneas al **conectar** cada cámara: el decoder entra a
  mitad de un GOP y se recupera en el siguiente keyframe. Pasa igual con
  cámaras reales.
- El VMS ya pedía RTSP por TCP (`main.py`), así que `stream_manager` no
  necesitó cambios.
- Observación de campo: Nahuel dio de alta la cámara 7 (UNIVIEW
  OEU-201S-HMK-W, por Wi-Fi, 192.168.0.17) con Cruce de Línea.
  - En 3 minutos hubo 34 errores de H.264 con el rig en 0 descartes, así que
    vienen de la cámara o del Wi-Fi, no del VMS.
  - Si se repite, conviene bajarle el bitrate en la cámara o mostrarla por el
    sub-flujo.
- Otros ajustes:
  - **YuNet ve el cuadro a 1280 px como máximo** (`MAX_DETECTION_SIDE`). El
    clip 2048x1536 pasó de 545 a 58 ms por cuadro. Con la carga completa,
    Rostros pasó de 1,5 a ~4 fps reales. Costo: 8 → 6 capturas en cam4 (todas
    caras reales) y 2 → 1 en cam2.
  - El aviso de OpenCV 5 `setPreferableTarget Targets are not supported by
    the new graph engine` sale en cada `FaceDetectorYN.create`. Se silencia
    solo durante ese create; el nivel de log se restaura.
  - **Tile** (`video_tile.frame_to_pixmap`): achica con OpenCV antes de pasar
    a Qt. La parte con el GIL tomado bajó de 1,4 a 0,04 ms por cuadro
    (2048x1536); el total no cambia (~2-4 ms). La imagen se ve más limpia
    (INTER_AREA en vez del vecino más cercano).
- Lo que sigue saliendo, a propósito:
  - Un aviso por analítica, "la inferencia tarda X ms y no alcanza los N fps
    configurados". En esta notebook (i5-1135G7, 4 núcleos / 8 hilos), con 5
    analíticas, cada una corre a ~5 fps sin importar si se configuran 15 o
    30. Configurarlas a 5-8 fps saca el aviso y no les resta nada.
  - El "Tips: QFluentWidgets Pro" es un `print` de la librería al importarse.

## Marcas inteligentes en el video

- Se dibujan en **Vista en Vivo** y en Vista Inteligente; antes, solo en esta
  última. Botón **"Marcas inteligentes"** en la barra superior de Vista en Vivo.
- **Incidente**:
  - Marco rojo que late sobre todo el video.
  - Banner "⚠ INCIDENTE · patada en la pantalla N".
  - La pantalla afectada queda con borde rojo, esquinas marcadas, rótulo
    "INCIDENTE · patada" y los puntos del golpe.
- La preferencia de marca se relee cada 2 s, no en cada cuadro.

## "Monitor roto" pasa a llamarse "Detección de incidentes"

- Solo el nombre visible: registro, diálogo, panel, dashboard, reglas y chips
  del video ("Incidentes").
- La clave interna sigue siendo `monitor_tamper` y la revisión sigue siendo
  `0007_monitor_roto`: renombrarlas sería otra migración, sin beneficio.

## Para Daniel (rig y rendimiento)

- `start_fake_cams.sh` re-encodea con libx264: son ~5 núcleos para 4 cámaras
  en esta notebook. Para las pruebas se publicó con `-c:v copy`. Propuesta:
  una variable `CODEC=copy` en el script.
- Si se edita `mediamtx.yml` con mediamtx corriendo, lo recarga y corta todas
  las sesiones. Los ffmpeg salen con error y hay que relanzarlos.
- Medido y **no aplicado** (sin mejora de fps, solo datos):
  - ONNX Runtime con `session.intra_op.allow_spinning=0`: los hilos del pool
    bajan de 2,7 a 1,6 núcleos, pero los fps de las analíticas no mejoraron.
  - Decoder con `CAP_PROP_N_THREADS=2`: hoy son 8 hilos por cámara (32 en
    total). Baja de 94 a 46 hilos, sin cambiar los descartes.

## Entorno

`DEV_nahuel/.vscode/settings.json` (fuera del repo) apunta el intérprete a
`AureaIA/.venv`. Así Pylance deja de marcar imports faltantes.

## Verificación

| Chequeo | Resultado |
|---|---|
| `ruff check` / `format --check` | ✅ / ✅ |
| `pytest` | ✅ **624 pasan** (+`test_smart_marks.py` 4, +`test_video_tile_render.py` 4) |
| Rig con las 5 analíticas, 60 s | ✅ 0 errores H.264, 0 descartes, analíticas a 4,9-5,6 fps |
