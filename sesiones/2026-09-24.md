# Sesión 2026-09-24 — Revisión de los commits de Nahuel y revisión 0008

Participantes: Daniel + Claude (Opus 5.5).

Plan aprobado: `/home/dani/.claude/plans/proud-enchanting-wren.md`. Primero la
**Fase R**, que revisa y arregla lo que entró el 23/09 a la noche y tiene dos
commits (R1 y R2). Después siguen las fases 2→7 del plan del 23/09
(`abundant-giggling-sunbeam.md`).

---

## Revisión de `8f4515b..2a1de5a` (Nahuel)

Tres commits: `e1f23d3` (rig de demo), `f309d79` (Detección de incidentes,
rostros por mejor captura, dashboard) y `2a1de5a` (bitácora).

La revisión se hizo con tres pasadas en paralelo:

- DB/Alembic;
- contratos de backend;
- estado de las bitácoras.

Cada hallazgo importante se confirmó a mano contra el código y contra una
**copia** de la base de desarrollo, que está en `0005_seguridad`. La base
real no se tocó.

### Lo que está bien

| Chequeo | Resultado |
|---|---|
| `ruff check` / `ruff format --check` | ✅ |
| `pytest -m "not integration" --cov` | ✅ 613 pasan, 89,23 % |
| `pytest -m integration` | ✅ 11 pasan |
| Cadena Alembic | lineal, una sola head (`0007_monitor_roto`, `down_revision = 0006`) |
| Sistema ad-hoc | no volvió; `TestNoVuelveElSistemaAdhoc` intacto |
| 0007 sobre copia de la base de dev | 0005→0007 ok, `integrity_check` ok, `foreign_key_check` vacío, ida y vuelta 0007↔0006 ok |
| Modelo RTMPose versionado (21,9 MB) | mismo criterio que YOLOX/YuNet; el `.spec` empaqueta `data/models` y el smoke lo carga |
| Referencias colgantes a `face_catalog` / `door_state_analyzer` | ninguna |
| `ultralytics` / `mmpose` / `mediapipe` | ninguno |

### Hallazgos

| # | Severidad | Qué |
|---|---|---|
| 1 | Importante | 0007 deja habilitadas configs de monitor **sin zonas ni ROI**. `MonitorTamperAnalyzer` exige zonas y levanta `ValueError`; `main._start_enabled_analytics` solo lo loguea, así que la UI la muestra activa y falla en cada arranque. **La config 4 de la base de dev cae en esto** (`door_state`, `params={}`). → **R1** |
| 2 | Importante | Alarma perdida en Detección de incidentes. `triggers` sale una sola vez por incidente, con la confianza del keypoint (desde 0,30), pero la regla filtra `min_confidence = 0.5`: si el primer golpe sale entre 0,30 y 0,49, el incidente no alarma nunca. → **R2** (test `xfail` + aviso a Nahuel) |
| 3 | Corrección de lo anotado el 23/09 | Las "3 UNIQUE faltantes" **existían**, pero sin nombre (`UNIQUE (name)`) y el metadata las espera como `uq_*`. La unicidad se cumplía. La deriva que cambia comportamiento es `devices.zone_id` sin `ON DELETE SET NULL`. → **R1** |
| 4 | Menor | `repository._RETIRED_ANALYZERS` mapea `motion_detection → monitor_tamper` y el comentario dice "estado de puerta". Se acepta el alias. → R2 |
| 5 | Menor | Código muerto `door_state` en `alarm_engine.py:55`. → R2 |
| 6 | Menor | `test_migracion_0006.py:127` compara revisiones como texto (`>=`). → R2 |
| 7 | Docs | La bitácora del 23/09 anuncia `FaceCatalog.observe`/`sightings()`/SFace, que no existen. El cambio de `metrics["heatmap"]` (lista → `ndarray`) no está declarado. `ARQUITECTURA.md` y `ROADMAP.md` siguen nombrando `door_state` y `face_catalog`. → R2 |
| 8 | Info | `face_catalog.py` (Daniel, `73382cf`/`1552bca`) lo borró Nahuel en `f309d79` por decisión de producto: ya no hay re-identificación ni conteo de únicos. Lo reemplazan `face_quality.py` y `ui/face_registry.py`. |
| 9 | Para Nahuel | RTMPose corre en cada cuadro por zona, sin prefiltro (~20 ms por zona); el modelo se carga en el hilo de la GUI; `FaceShot` ocupa mucha memoria; la descarga del zip de pose no es atómica. Sospecha de licencia: los pesos *body7* se entrenaron con datasets de uso no comercial, y no hay LICENSE/NOTICE de terceros. → R2 (ROADMAP) |

### Decisiones de Daniel

| Tema | Decisión |
|---|---|
| Arreglo de 0007 | Una sola revisión **0008** que apaga los monitores sin pantalla **y** normaliza las bases adoptadas. 0007 no se edita: ya está pusheada y una base en head no recibiría el arreglo. |
| FK `analytics_configs.device_id` ausente en bases pre-baseline | Entra en 0008. Las configs huérfanas se borran, que es lo que habría hecho el `CASCADE`. |
| Alias `motion_detection → monitor_tamper` | Se acepta y se documenta. |
| Alarma perdida | Test que la demuestra (`xfail strict`) + aviso a Nahuel; su analítica no se toca. |
| Orden | Fase R (R1, R2) y después 2→7. |

**Regla hasta la Fase 2:** no arrancar la app sobre `data/aurea_vms.sqlite3`.
Saltaría de 0005 a 0008 sin backup automático. Toda verificación va sobre copias.

---

## Fase R1 — Revisión `0008_normaliza_adoptadas`

`aurea_vms/migrations/versions/0008_normaliza_adoptadas.py`, colgada de
`0007_monitor_roto`.

### Qué hace

1. **Precondición:** si `PRAGMA foreign_keys` está ON, levanta antes de tocar
   nada. Con FKs activas, el `DROP` de la tabla vieja que hace el batch borra
   en cascada a los hijos. Hoy el engine de migración de `env.py` no aplica
   el listener de pragmas y queda en OFF (el default de SQLite). La Fase 2 lo
   va a forzar en `env.py`, y esto queda como segunda red.
2. **Datos:** los `monitor_tamper` **habilitados**, sin ninguna zona de 4
   valores y sin ROI completo, pasan a `enabled=0` con la marca
   `params["deshabilitada_0008"] = true`. El criterio es el mismo que usa
   `registry.create_analyzer`. Los que ya estaban apagados no se marcan: si
   se marcaran, el downgrade los prendería. El downgrade rehabilita solo los
   marcados. `params` se bindea como `sa.JSON`, que es portable.
3. **Esquema, condicional** (en una base nueva no hace nada):
   - FKs `devices.zone_id → zones` (`SET NULL`) y `analytics_configs.device_id → devices` (`CASCADE`): si falta la FK, no tiene nombre o no tiene el `ondelete` correcto, se recrea con batch y `naming_convention`. Con esa convención, el batch le pone a la FK anónima reflejada el mismo nombre que la convención, y así se la puede reemplazar. Las filas huérfanas se resuelven como lo habría hecho la FK (NULL o borradas) y se loguea cuántas fueron.
   - UNIQUE `sites.name`, `users.username` y `media_assets.rel_path`: si existe sin nombre, `recreate="always"` con la convención le pone el nombre. Si no existe y hay repetidos, `RuntimeError` con los valores, en vez de un `IntegrityError` anónimo.
   - La convención está copiada y congelada en la revisión: una revisión no depende del código vivo.
   - El downgrade de esquema no vuelve a la deriva.

Experimento previo en el scratchpad: el batch con `naming_convention` sobre
la copia de la base de dev deja `compare_metadata` vacío.

### Tests

`tests/test_migracion_0008.py`, 18 tests:

- **Monitores:**
  - sin zonas ni ROI (la forma real de la base de dev) → apagado con marca;
  - con zonas → sigue habilitado;
  - con ROI → sigue habilitado;
  - zonas mal formadas no cuentan como pantalla;
  - uno ya apagado no se marca;
  - las demás analíticas no se tocan;
  - el downgrade rehabilita solo los marcados;
  - la ida y vuelta deja lo mismo.
- **Base adoptada** (la `ESQUEMA_VIEJO` de `test_db_migrations`, adoptada y llevada a 0007):
  - trae deriva antes, para que el test siguiente no pase sobre una base sin nada que normalizar;
  - después de 0008, **`compare_metadata == []`**. Es el test que faltaba: el existente solo cubría bases nuevas;
  - borrar una zona deja la cámara sin zona;
  - recrear `devices` no borra canales, configs, reglas ni media;
  - la config huérfana se borra;
  - `zone_id` huérfano queda en NULL;
  - una base nueva no cambia (mismo `sqlite_master`);
  - sin UNIQUE y con repetidos falla con el mensaje;
  - sin UNIQUE y sin repetidos lo crea.
- **Precondición:** con `foreign_keys=ON` se niega a migrar, y la base queda en 0007 y sin tocar.

Ajustes:

- `test_db_migrations.py::test_renombra_el_analizador_de_movimiento`: la config legada termina `monitor_tamper` **deshabilitada**. Antes fijaba el caso roto.
- `test_migracion_0007.py::test_una_base_nueva_llega_a_esta_revision`: comparaba contra `"0007"` fijo y se rompió al sumar 0008. Ahora compara contra la cabeza real y comprueba que 0007 esté en la cadena.

### Los tests prueban algo

Cada pieza se revirtió a mano sobre la revisión y después se restauró
(`cmp` contra el original).

| Pieza revertida | Tests que fallan |
|---|---|
| A. No apagar monitores | `sin_zonas_ni_roi…`, `zonas_mal_formadas…`, `test_db_migrations::…movimiento` (3) |
| B. No normalizar FKs | `coincide_con_los_modelos`, `borrar_una_zona…`, `config…que_ya_no_existe…`, `zona_que_ya_no_existe…` (4) |
| C. No normalizar UNIQUE | `coincide_con_los_modelos`, `…con_repetidos…`, `…sin_repetidos_lo_crea` (3) |
| D. Sin precondición de FKs | `con_foreign_keys_activas_se_niega_a_migrar` (1) |
| E1. Huérfanas `CASCADE` sin borrar | `config…que_ya_no_existe_se_borra` (1) |
| E2. Huérfanas `SET NULL` sin anular | `zona_que_ya_no_existe_queda_sin_zona` (1) |
| F. Downgrade que no prende | `downgrade_rehabilita_solo_los_marcados` (1) |
| G. Marcar también los ya apagados | `uno_ya_deshabilitado…`, `downgrade_rehabilita…` (2) |

### Verificación end-to-end (copias en el scratchpad)

`migrar()` sobre copias de tres bases reales:

| Base | Desde | Hasta | Filas (8 tablas) | Deriva | `integrity` / `fk_check` | Config 4 | 0008↔0007 |
|---|---|---|---|---|---|---|---|
| `aurea_vms.sqlite3` | 0005 | 0008 | iguales | 0 | ok / vacío | `enabled=0`, marca | ok |
| `.bak-pre-alembic` | sin versionar (adopción) | 0008 | iguales | 0 | ok / vacío | `enabled=0`, marca | ok |
| `.bak-pre-0004` | 0003 | 0008 | iguales | 0 | ok / vacío | `enabled=0`, marca | ok |

El downgrade a 0007 devuelve la config 4 a `enabled=1`, `{}` exacto.

### Cierre de fase

| Chequeo | Resultado |
|---|---|
| `ruff check` / `ruff format --check` | ✅ |
| `pytest -m "not integration" --cov` | ✅ **631 pasan** (+18), 89,23 % |
| `pytest -m integration` | ✅ 11 pasan |
| Cobertura de `0008_normaliza_adoptadas.py` | 93 %. Faltan ramas defensivas de `_cargar` y el corte no-SQLite. Ojo: `migrations/versions` no tiene `__init__` y no entra en el total global; ya era así. |
