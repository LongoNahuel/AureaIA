# Sesión 2026-09-22 — Fases 3, 4 y 5 (bloqueantes de robustez)

Continuación de `sesiones/2026-09-21.md`. Del plan de 11 fases acordado ayer
están hechas la 0, la 1 y la 2 (`7478e66`). Hoy se cierran las tres que quedan
del bloque "bloqueantes de demo": B3, B4 y B5.

Participantes: Daniel + Claude (Opus 5).

Hilo conductor de las tres fases: **hoy el backend miente o se cuelga en silencio
cuando algo sale mal**. Una notificación construye un widget de Qt desde un hilo
de analítica; un clip que no se pudo escribir queda indexado como evidencia
válida; un stream congelado no se reconecta nunca. Al terminar el día, cada uno
de esos tres caminos tiene que fallar de forma visible, acotada y testeada.

---

## Fase 0 — Reparación del entorno y línea base

### El entorno de esta máquina no era el de la bitácora de ayer

Todos los archivos del repo tienen mtime de hoy 08:34 (checkout fresco), pero el
`.venv` quedó del **24/08**: `site-packages` sin tocar desde entonces. Resultado:

```
$ .venv/bin/python -c "import aurea_vms.core.analytics.registry"
ModuleNotFoundError: No module named 'onnxruntime'
```

`onnxruntime` es dependencia declarada (`pyproject.toml:17`) e import de módulo
(`object_detector_backend.py:33`), así que **no corría ni la suite ni el smoke**.
El venv todavía tenía `mediapipe 0.10.35`, sacado del proyecto en `1d78e77`.

Los modelos sí estaban donde corresponde: `data/models/yolox_tiny.onnx` (20 MB) y
`face_detection_yunet_2023mar.onnx` (228 KB), ambos versionados.

Se reparó con `.venv/bin/pip install -e ".[dev]"` → `onnxruntime 1.30.0` +
`protobuf 7.36.2`. `mediapipe` queda instalado pero huérfano: no lo importa nadie
y desinstalarlo no aporta nada.

### Línea base, en verde y coincidente con ayer

| Chequeo | Resultado |
|---|---|
| `ruff check aurea_vms tests` | ✅ All checks passed |
| `ruff format --check` | ✅ 129 archivos ya formateados |
| `pytest -m "not integration" --cov` | ✅ **291 pasan**, cobertura **81.80%** (gate 80) |
| `pytest -m integration` | ✅ 1 pasa, 2.13s |
| `python -m aurea_vms.main --smoke` | ✅ door_state, people_counting, line_crossing, face_detection |

Coincide exactamente con el cierre de la Fase 2 de ayer (291 / 81.80%). Esta es
la referencia contra la que se compara todo lo de hoy.

### Se reprodujo el bug de `model_assets` documentado ayer

El smoke se corrió con `AUREA_DATA_DIR` apuntando a un tmp, y **bajó los dos
modelos de internet** en vez de tomarlos del repo:

```
[WARNING] model_assets: Modelo yolox_tiny.onnx ausente: descargando de https://github.com/...
[WARNING] model_assets: Modelo face_detection_yunet_2023mar.onnx ausente: descargando de https://...
```

Es exactamente lo anotado ayer: `resources.bundled_path("models/x")` resuelve a
`PROJECT_ROOT/models/x` en desarrollo (`config/resources.py:23-25`), pero los
modelos viven en `PROJECT_ROOT/data/models/`, así que el paso 2 de `ensure_model`
("copiar del bundle") nunca acierta y se cae al paso 3 (descarga). Queda
confirmado con evidencia; sigue en el ROADMAP, fuera del alcance de hoy.

### Bitácoras de agosto, versionadas

`sesiones/2026-08-24.md`, `2026-08-28.md` y `2026-08-29.md` estaban sin versionar
pese a la decisión de ayer de que las bitácoras viajan con el repo (la Fase 0 de
ayer sacó `sesiones/` del `.gitignore`, pero solo se commiteó el archivo del
día). Entran en el mismo commit que este archivo.

---

## Fase 3 — La notificación de escritorio, en el hilo de Qt (B3)

### El bug

`AlarmEngine` no es `QObject` y se conecta a `event_bus.detection` sin
`ConnectionType` (`alarm_engine.py:43`), así que `_on_detection` corre en el hilo
del `AnalyticsWorker` que emitió. Desde ahí, `_trigger` llamaba a
`desktop_notify.notify()`, que **construye un `QSystemTrayIcon`** — un widget de
Qt — fuera del hilo de la GUI. Según la plataforma eso es un crash o una
notificación que nunca aparece; en los dos casos, comportamiento indefinido. El
icono vivía además en un global de módulo con doble-check sin lock.

### El arreglo: el mismo camino que ya usaba `play_sound`

El proyecto ya tenía resuelto este problema para el beep de alarma, y estaba
documentado en `events.py`: *"la UI (hilo principal) reproduce el beep — los
engines no tocan audio"*. Se aplicó la misma forma, que es la regla general del
`EventBus` (los engines publican, la UI consume):

- `events.py` — `AlarmEvent` suma `notify_desktop: bool = False`.
- `alarm_engine.py` — deja de importar `desktop_notify` y marca la acción en el
  DTO. **Se cae también un `repository.get_device()` del hilo de analítica**: se
  usaba solo para armar el texto de la notificación, y
  `MainWindow._on_global_alarm` ya resolvía el nombre de la cámara por su cuenta
  (`main_window.py:215-216`). Una query menos por alarma en el hilo caliente.
- `main_window.py` — `_on_global_alarm` (conectado con `QueuedConnection`)
  dispara la notificación con el mismo texto de antes.

### Y un guard para que el bug no pueda volver

Mover la llamada arregla el caso de hoy, pero no impide que mañana alguien
vuelva a llamar a `notify()` desde un worker. Por eso `desktop_notify` quedó con
una guarda explícita:

```python
def _is_gui_thread() -> bool:
    app = QApplication.instance()
    if app is not None:
        return QThread.currentThread() is app.thread()
    return threading.current_thread() is threading.main_thread()
```

Con la `QApplication` viva se usa la definición exacta de Qt; sin ella (tests,
import temprano) se cae al hilo principal de Python, que es donde `main()`
termina creándola. Una llamada desde otro hilo **loguea el nombre del hilo
culpable y no construye nada**.

De paso, dos no-ops mudos que tenía el módulo pasaron a dejar rastro: "no hay
`QApplication`" (debug) y "el escritorio no expone bandeja del sistema"
(warning). Antes la regla decía "notificar", no notificaba, y no había una sola
línea en el log para darse cuenta. Y el icono se crea con la app como padre, así
que muere con ella en vez de quedar vivo hasta el fin del intérprete.

### Los tests discriminan (se verificó revirtiendo el fix)

Es la lección de la Fase 2, donde los tests de concurrencia pasaban con y sin el
arreglo. Se revirtió cada mitad a mano:

| Fix revertido | Resultado |
|---|---|
| Guard de hilo desactivado (`if False:`) | ❌ falla `test_notify_desde_otro_hilo_no_construye_el_widget` |
| `desktop_notify` de vuelta en el engine | ❌ fallan `test_notify_desktop_viaja_como_flag_del_dto` y `test_el_engine_no_conoce_el_modulo_de_notificaciones` |

`tests/test_desktop_notify.py` es nuevo (10 tests): guard de hilo en sus tres
caminos (con app, sin app, desde un hilo secundario), el globo que sí se muestra,
el icono que se crea una sola vez y con padre, y los dos casos sin bandeja. Los
dobles reemplazan `QApplication`/`QSystemTrayIcon` en el namespace del módulo
en vez de depender del entorno: bajo `QT_QPA_PLATFORM=offscreen`,
`isSystemTrayAvailable()` no es determinista y un test que dependiera de eso
pasaría o fallaría según la máquina.

En `tests/test_alarm_trigger.py`, `test_notify_desktop` cambió de contrato
(antes contaba llamadas al mock, ahora verifica el flag del DTO) y se sumó
`test_el_engine_no_conoce_el_modulo_de_notificaciones`, que es una guarda de
regresión chata pero efectiva: `assert not hasattr(ae_module, "desktop_notify")`.

### Verificación

| Chequeo | Resultado |
|---|---|
| `ruff check aurea_vms tests` | ✅ |
| `ruff format --check` | ✅ 130 archivos |
| `pytest -m "not integration" --cov` | ✅ **303 pasan**, cobertura **82.51%** |
| `pytest -m integration` | ✅ |
| `python -m aurea_vms.main --smoke` | ✅ los 4 analizadores |
| Tests contra el fix revertido | ✅ 3 fallan (ver tabla) |

Línea base anterior: 291 tests / 81.80%. Ahora 303 / 82.51% (+12 tests).
`core/desktop_notify.py` pasó de **29% a 100%** de cobertura.

### Archivos tocados

- `aurea_vms/core/events.py` — el flag `notify_desktop` en el DTO.
- `aurea_vms/core/alarm_engine.py` — sin `desktop_notify`, sin la query de
  cámara; docstring del módulo actualizado con el invariante.
- `aurea_vms/core/desktop_notify.py` — guard de hilo, logger, icono con padre.
- `aurea_vms/ui/main_window.py` — `_on_global_alarm` ejecuta la acción.
- `tests/test_desktop_notify.py` — nuevo.
- `tests/test_alarm_trigger.py` — contrato nuevo + guarda de regresión.
- `docs/ROADMAP.md` — B3 a "Hecho"; `desktop_notify` sale de la lista de módulos
  sin cobertura; se anota que `main()` nunca llama a `app.setWindowIcon()`, así
  que el globo sale con icono nulo (cosmético, lane de Nahuel).

---

## Fase 4 — Evidencia que no miente (B4)

### El bug: tres escrituras que nadie verificaba

`media_assets` es el **único** índice de evidencia — nada escanea las carpetas
(es una decisión explícita del proyecto, está en el docstring de
`media_store.py`). O sea que una fila que apunta a un archivo inexistente o
vacío es peor que no tener la fila: el operador la ve listada como evidencia
válida de un incidente y se entera recién al abrirla, cuando ya no hay nada que
recuperar.

Y `cv2` no avisa por excepción en ninguno de los dos caminos:

1. **`cv2.imwrite` devuelve `False`** ante un disco lleno o un formato que el
   build no sabe codificar. El retorno se descartaba y se registraba igual.
2. **`cv2.VideoWriter` devuelve un writer CERRADO** ante un códec ausente: los
   `write()` son no-op, el archivo queda vacío, y `register()` insertaba la fila
   lo mismo.
3. **`media_store.register()`** hacía `.stat()` sin guarda y no miraba el tamaño.

Más dos detalles: `_write_mp4` reventaba con `AttributeError: 'NoneType' object
has no attribute 'shape'` si el primer JPEG estaba corrupto, y `duration_s` se
calculaba con los frames **intentados**, así que un clip con frames corruptos
declaraba más duración de la que tenía.

### Lo que no estaba en el informe: se perdía la alarma entera

El hallazgo más grave apareció al trazar el camino de la excepción. Si
`save_snapshot` fallaba, el error subía desde `alarm_engine.py:151` al `except`
por regla de `_on_detection` — o sea **`event_bus.alarm.emit` nunca corría y el
incidente no llegaba a la UI**. Se perdía la alarma completa por no haber podido
escribir un `.jpg`.

Se reprodujo al revés, revirtiendo la guarda:

```
E  AttributeError: 'NoneType' object has no attribute 'rel_path'
```

Ahora `save_snapshot` **no levanta nunca**: devuelve `None` con el motivo en el
log, `_trigger` deja `snapshot_path=None` (camino que el DTO ya soportaba) y el
incidente sigue su curso. El JPEG es el adorno; la alarma es el dato.

### Lo que no se pudo escribir, se borra

Cada camino de falla llama a `_discard()`, que loguea y **borra el archivo a
medias**. No es cosmético: como `media_assets` es el único índice, un archivo que
no se indexa queda invisible para el `RetentionWorker` y no lo limpia nunca
nadie. Se cambia una fila mentirosa por cero bytes de basura, no por basura
silenciosa.

### `wait_for_pending(15.0)` contra `clip_post_seconds`

El `15.0` estaba hardcodeado en `main.py:103` mientras `clip_post_seconds` vale
10. Subirlo por encima de 15 truncaba clips en **cada** apagado, y un `.mp4`
cortado antes del `writer.release()` no tiene el átomo `moov`: es
irreproducible. Ahora el tope se deriva de settings más un margen
(`CLIP_SHUTDOWN_MARGIN_S = 5.0`).

### Los tests discriminan (se verificó revirtiendo cada chequeo)

| Chequeo revertido | Resultado |
|---|---|
| Retorno de `cv2.imwrite` ignorado | ❌ `test_imwrite_fallido_no_indexa_ni_deja_archivo` |
| `writer.isOpened()` sin chequear | ❌ `test_video_writer_que_no_abre_no_indexa` |
| `duration_s` con los frames intentados | ❌ `test_duration_s_cuenta_los_frames_escritos_no_los_intentados` |
| `size_bytes == 0` sin validar | ❌ `test_archivo_vacio_no_se_indexa` |
| Guarda de `first_frame is None` | ❌ `AttributeError: 'NoneType' object has no attribute 'shape'` |
| `_trigger` sin tolerar `asset is None` | ❌ `AttributeError: 'NoneType' object has no attribute 'rel_path'` |

11 tests nuevos: `TestEvidenciaQueNoMiente` y `TestWaitForPending` en
`test_clip_recorder.py`, `TestRegister` en `test_media_store.py` (que no tenía
ningún test de `register()`), y el que más importa,
`test_una_captura_fallida_no_se_lleva_puesta_la_alarma` en
`test_alarm_trigger.py`.

El doble `FakeVideoWriter` reproduce el modo de falla real: no lanza, devuelve
`isOpened() == False` y traga los `write()`.

### Verificación

| Chequeo | Resultado |
|---|---|
| `ruff check aurea_vms tests` | ✅ |
| `ruff format --check` | ✅ 130 archivos |
| `pytest -m "not integration" --cov` | ✅ **314 pasan**, cobertura **82.65%** |
| `pytest -m integration` | ✅ |
| `python -m aurea_vms.main --smoke` | ✅ los 4 analizadores |
| Tests contra los fixes revertidos | ✅ 6 fallan (ver tabla) |

Fase 3 cerró en 303 tests / 82.51%. Ahora 314 / 82.65% (+11 tests).

### Archivos tocados

- `aurea_vms/core/media_store.py` — `logger`, `MediaWriteError`, validación de
  existencia y tamaño en `register()`.
- `aurea_vms/core/clip_recorder.py` — helpers `_decode` y `_discard`;
  `save_snapshot` y `_write_mp4` devuelven `MediaAsset | None`;
  `wait_for_pending` deriva el tope de settings; `_record_clip` no anuncia un
  clip que no existe.
- `aurea_vms/core/alarm_engine.py` — `_trigger` tolera una captura fallida.
- `aurea_vms/main.py` — `wait_for_pending()` sin el 15.0 fijo.
- `tests/test_clip_recorder.py`, `tests/test_media_store.py`,
  `tests/test_alarm_trigger.py`.
- `docs/ROADMAP.md` — B4 a "Hecho".

---

## Fase 5 — Streams que se recuperan solos (B5)

### Los dos bugs

**Reconexión sin backoff.** `RECONNECT_DELAY_S = 3.0`, fijo y para siempre, en
los dos puntos de espera del loop. Sabiendo que `cv2.VideoCapture` bloquea hasta
`OPEN_TIMEOUT_MS = 10_000`, contra una cámara realmente apagada eso es un ciclo
de ~13 s: unas 275 aperturas de socket por hora **y por cámara**, todas
condenadas a fallar, hasta que alguien la vuelva a enchufar.

**Ningún watchdog de congelado.** Un decoder colgado sigue devolviendo
`ok=True` con el **mismo frame ya decodificado**, así que el corte por `read()`
fallido no llega nunca. La cámara queda mostrando una foto vieja que parece en
vivo — exactamente lo que `is_stale()` existía para detectar, pero
`is_stale()` solo lo consumía la UI (`video_tile.py:306`) para pintar el tile:
nadie reconectaba.

### Backoff exponencial con tope y jitter

```python
def _reconnect_delay(attempt: int, *, jitter: bool = True) -> float:
    base = min(RECONNECT_DELAY_S * 2 ** max(0, attempt - 1), RECONNECT_MAX_DELAY_S)
    if not jitter:
        return base
    return base * (1.0 + random.random() * RECONNECT_JITTER)
```

Curva: 3 → 6 → 12 → 24 → 30 → 30. El jitter (hasta +25%) es para que N cámaras
que se caen juntas — un switch que se reinicia es el caso típico — no se queden
reintentando en fase para siempre.

**El detalle que importa: cuándo se resetea el contador.** Resetear ante
`isOpened()` es el atajo tentable y está mal: una cámara que abre el socket y se
muere al instante (firmware colgado, NVR saturado) nunca saldría del delay
mínimo y seguiríamos con los 3 s de siempre. Se resetea solo si la conexión
**entregó frames**:

```python
attempt = 1 if delivered_frames else attempt + 1
```

### Watchdog de frame congelado

Se comparó una firma submuestreada, 1 de cada 32 píxeles por eje (~200 valores
en 1080p), y se corta **solo si es idéntica por más de 15 s seguidos**. El doble
requisito es a propósito: el rig de demo usa mp4 en loop y puede tener tramos
estáticos, mientras que con ruido de sensor real 15 s de frames byte-idénticos
no existen. Cada disparo deja un `logger.warning` con cuántos segundos llevaba,
para poder distinguir un falso positivo de una cámara colgada sin adivinar.

Un congelado **no castiga el backoff**: entregó frames, así que el próximo
intento sale a los 3 s. Es un glitch recuperable, no una cámara muerta.

El `run()` quedó partido en tres: `run()` (el ciclo de conexión),
`_wait_before_retry()` (reporta estado y espera) y `_capture_loop()` (lee frames
y decide si se cortó o se congeló, devolviendo `(entregó_frames, congelado)`).

### Los tests: el primer doble de `cv2.VideoCapture` del repo

`StreamWorker.run()` no se ejecutaba en **ningún** test — el loop entero estaba
en 0% — porque no existía un doble de `cv2.VideoCapture`. Se sumaron
`FakeCapture` (scripteable: falla al abrir / entrega N frames / entrega el mismo
frame) y `FakeStopEvent` (corta el loop tras N esperas y deja registrado cuánto
se pidió esperar en cada una), que es lo que permite asertar la curva del
backoff desde adentro del loop real.

17 tests nuevos. Se verificó que discriminan revirtiendo cada mitad:

| Fix revertido | Tests que fallan |
|---|---|
| Delay fijo en vez de exponencial | 5 (`TestBackoff` × 2, `TestReconexion` × 3) |
| Watchdog de congelado desactivado | 2 (`TestWatchdogDeCongelado`) |
| Reset del backoff con "abrió" en vez de "entregó frames" | 1 (`test_abrir_sin_entregar_frames_no_resetea_el_backoff`) |

### Verificación con cv2 real

Además de la suite, se corrió un `StreamWorker` de verdad contra un puerto
cerrado (`rtsp://127.0.0.1:1/nada`):

```
10:20:58 WARNING Camara 99 (main): No se pudo abrir el stream (intento 1, reintenta en 4s)
10:21:02 WARNING Camara 99 (main): No se pudo abrir el stream (intento 2, reintenta en 7s)
10:21:09 WARNING Camara 99 (main): No se pudo abrir el stream (intento 3, reintenta en 13s)
--- stop() -> hilo vivo=False en 0.00s ---
```

4 → 7 → 13 son las bases 3/6/12 con el jitter encima. Y lo segundo importa
tanto como lo primero: `stop()` corta el hilo **en el acto** aunque esté en
medio de una espera de 30 s, así que el `stop_all()` + `join(timeout=2.0)` del
apagado ordenado (`main.py:100-109`) sigue cerrando en tiempo.

### Lo que NO se tocó, a propósito

Los estados persistidos siguen siendo `online`/`offline`/`unknown`. Agregar
"reconectando" se propaga al donut del dashboard
(`ui/widgets/dashboard_panel.py:156-158`) y es otra fase. El `detail` del
`DeviceStatusEvent` sí lleva ahora el motivo y el próximo delay, que es lo que
hace diagnosticable el problema desde el log.

Tampoco hizo falta cablear `is_stale()` en la analítica: `analytics_engine.py`
ya descarta el frame repetido por su dedup de timestamp.

### Verificación

| Chequeo | Resultado |
|---|---|
| `ruff check aurea_vms tests` | ✅ |
| `ruff format --check` | ✅ 130 archivos |
| `pytest -m "not integration" --cov` | ✅ **328 pasan**, cobertura **84.46%** |
| `pytest -m integration` | ✅ |
| `python -m aurea_vms.main --smoke` | ✅ los 4 analizadores |
| Tests contra los fixes revertidos | ✅ 8 fallan (ver tabla) |
| Backoff real contra un puerto cerrado | ✅ 4 s → 7 s → 13 s, stop() en 0,00 s |

Fase 4 cerró en 314 tests / 82.65%. Ahora 328 / 84.46% (+14 tests).
`core/stream_manager.py` pasó de **70% a 92%**.

### Archivos tocados

- `aurea_vms/core/stream_manager.py` — `_reconnect_delay`, `_frame_signature`,
  `_wait_before_retry`, `_capture_loop`, y `run()` con el contador de intentos.
- `tests/test_stream_manager.py` — `FakeCapture`, `FakeStopEvent` y las cuatro
  clases de test nuevas.
- `docs/ROADMAP.md` — B5 a "Hecho".

---

## Cierre del día

Las tres fases del bloque "bloqueantes de robustez" quedaron cerradas. La
sección **Robustez (bloqueantes de demo)** del ROADMAP quedó con un solo ítem,
sin marcas 🟠: los hilos daemon sin techo de `device_manager`, que va con la
Fase 6 (T4) porque toca el mismo código muerto.

| Fase | Commit | Tests | Cobertura |
|---|---|---|---|
| línea base | `7478e66` | 291 | 81.80% |
| 3 — notificación en el hilo de Qt (B3) | `32ca58a` | 303 | 82.51% |
| 4 — evidencia que no miente (B4) | `91a3897` | 314 | 82.65% |
| 5 — streams que se recuperan solos (B5) | — | **328** | **84.46%** |

+37 tests y +2,66 puntos de cobertura en el día, con `desktop_notify` de 29% a
100% y `stream_manager` de 70% a 92%.

### Pendiente / arranque de la próxima

Fases 6 a 11 del plan acordado el 2026-09-21, en ese orden:

6. Red de tests contra regresiones de inferencia (T1 + T4).
7. Lógica de dominio fuera de los widgets (T2 + T3 + T6).
8. Alembic y versionado de esquema (D1 + D2).
9. Constraints e índices que faltaban (D3-D6).
10. Seguridad pre-cliente (S1-S3).
11. Una sola sesión ONNX por modelo (P1).

Dos decisiones que hay que tomar **antes** de empezar esas fases:

- **Fase 8**: qué hacer con las DBs de campo que ya existen (la de desarrollo de
  Dani entre ellas). Baseline con `alembic stamp` + una revisión inicial que
  refleje el esquema actual, y qué pasa con `_ADHOC_COLUMNS`: ¿se borra o queda
  como red de seguridad?
- **Fase 10**: qué dependencia de cripto entra para S1 (`cryptography` con una
  clave derivada localmente, `keyring` del SO, o DPAPI en Windows). Hoy el
  proyecto no tiene **ninguna** librería de cripto declarada; lo único
  disponible es `hashlib` + `os.urandom` de la stdlib.

Y sigue abierto, ya con evidencia de hoy, el bug de `model_assets`: con
`AUREA_DATA_DIR` redirigido los modelos se bajan de internet en vez de tomarse
del bundle. Afecta al `build-windows.yml`, que corre el smoke con un data-dir
vacío en cada ejecución.

---

## Extra — El seed de demo, roto en cualquier máquina limpia

Salió mientras se planificaban las fases 6-11, auditando los call-sites de los
`update_*`/`add_*` contra las columnas reales de cada modelo (el análisis que
pide la Fase 9 para la whitelist de kwargs). De los 11 call-sites sospechosos,
10 son `**dict` dinámico desde los diálogos — imposibles de auditar
estáticamente — y **uno era un bug real**:

```
tools/demo/seed_demo.py:76   add_device   kwarg desconocido: 'site_id'
```

`Device` perdió la columna `site_id` en la migración a zonas (`3977978`, 29/08).
Y el constructor declarativo de SQLAlchemy **sí** valida kwargs:

```
TypeError: 'site_id' is an invalid keyword argument for Device
```

O sea que el seed no fallaba en silencio: moría en la primera cámara. Quedó
escondido porque solo se ejecuta ese branch al crear una cámara **nueva**, y en
la DB de desarrollo las 4 ya existían desde el 24/08. En una notebook limpia —
la de la demo — el rig no se puede sembrar desde hace casi un mes.

Dato útil de lado, que reordena la Fase 9: los `add_*` ya validan kwargs; los
que tragan cualquier cosa en silencio son **solo** los seis `update_*` y el
branch de update de `upsert_analytics_config`, porque usan `setattr`.

### El arreglo

Las cámaras se cuelgan de una zona, no de un sitio. El seed crea o reusa una
zona por sitio con el mismo nombre `"General"` que usa el backfill de
`models/db.py:194-202`, para que las dos rutas converjan en la misma zona en vez
de crear dos.

Se sumó además un caso que el script no cubría: una cámara que ya existe pero
quedó **sin zona** (sembrada por una versión anterior, o migrada a medias) se
reasigna. Sin eso es invisible para el filtro global de sitio y la demo arranca
con la grilla vacía. Solo se toca si `zone_id is None`, para no pisar una
asignación hecha a mano.

### Verificación

`tests/test_seed_demo.py` es nuevo y es el primer test de `tools/` del repo. Se
carga el script por ruta porque `tools/` no es un paquete. 6 tests: siembra con
zona, visibilidad por el filtro global de sitio, una zona por sitio,
idempotencia, reasignación de una cámara huérfana, y que no pise una zona
asignada a mano.

Revirtiendo el `zone_id` a `site_id` fallan 4 con el `TypeError` exacto.

Y la prueba que importa, el script real contra un data-dir vacío:

```
$ AUREA_DATA_DIR=<tmp vacío> python tools/demo/seed_demo.py
+ sitio: Sala Principal
+ sitio: Anexo VIP
+ zona: Sala Principal / General
+ zona: Anexo VIP / General
+ cámara: Mesas 01 -> rtsp://127.0.0.1:8554/cam1
...
Seed OK.                                  (segunda corrida: Seed OK, sin duplicar)

Anexo VIP:      ['Bóveda Acceso', 'Zona Sur Sala']
Sala Principal: ['Mesas 01', 'Acceso Hall']
```

| Chequeo | Resultado |
|---|---|
| `ruff check aurea_vms tests tools` | ✅ |
| `ruff format --check` | ✅ 131 archivos |
| `pytest -m "not integration" --cov` | ✅ **334 pasan**, cobertura **84.51%** |
| `pytest -m integration` | ✅ |
| `python -m aurea_vms.main --smoke` | ✅ los 4 analizadores |
| Seed en data-dir vacío, dos corridas | ✅ |

Nota para la Fase 10: estos 6 tests tardan ~4 s, casi todo PBKDF2 sembrando los
4 usuarios. Al subir las iteraciones a 600.000 esto se va a más que duplicar —
habrá que bajar el coste en el entorno de tests.

---

## Fase 6 — Red de tests contra regresiones de inferencia (T1 + T4)

### El agujero

El ROADMAP lo decía con todas las letras: *"una regresión en el letterbox, la
decodificación de cabezas, el NMS o YuNet no la detecta nadie"*. Y era cierto
por partida doble: los unitarios de los analizadores reemplazan `YoloxDetector`
**entero** por un doble (`ScriptedDetector`), y el único test de integración
construía los 4 analizadores y llamaba `close()` sin correr una sola inferencia
— de ahí que tardara 0,62 s. Todo lo que hay entre el frame y las cajas no lo
ejercitaba nada: `object_detector_backend` estaba al **40%**.

Esa clase de regresión no se manifiesta como excepción. Se manifiesta como cajas
corridas medio relleno, que en un video mirado de reojo pasan por buenas.

### `tests/test_yolox_pipeline.py` — 25 tests, sin cargar el modelo

Los tensores se fabrican a mano. Lo que se fija:

- **Letterbox**: salida CHW 416×416 float32, una sola escala para los dos ejes
  (si se escalara cada uno por su cuenta las cajas volverían deformadas), el
  relleno gris 114 **abajo y a la derecha** — la convención del export oficial de
  YOLOX es alinear arriba-izquierda, y si se centrara el `/= scale` de `detect()`
  devolvería todo corrido —, y que **no se normaliza a 0-1** (el modelo espera
  0-255; normalizar daría detecciones vacías sin ningún error visible).
- **Round-trip del letterbox**: se dibuja un rectángulo en un frame de 640×480,
  se busca dónde quedó en la imagen 416 y se verifica que dividiendo por la
  escala vuelve a su lugar dentro de 3 px. Es el invariante del que depende
  `boxes_xyxy /= scale`.
- **Decodificación**: las tres cabezas (2704 + 676 + 169 = 3549 anchors), que
  cada una decodifica con su stride, que **la grilla avanza primero en x** (si se
  invirtiera el meshgrid las cajas saldrían transpuestas y no lo detectaría nadie
  salvo mirando el video), y que el tamaño es exponencial.
- **Un filo documentado**: `_decode_outputs` **escribe sobre el array que
  recibe**. Llamarlo dos veces sobre la misma salida la decodifica dos veces.
  Hay un test que lo deja fijado para el próximo que lo toque.
- **NMS**: supresión por IoU con la convención `+1` del demo oficial (dos cajas
  de 10×10 solapadas a la mitad dan IoU 0.175, así que el umbral 0.10 suprime y
  el 0.50 no), y que `_multiclass_nms` es **class-agnostic**: dos cajas
  superpuestas de clases distintas se suprimen igual entre sí.

### El test que más vale: `detect()` de punta a punta

`TestDetectDePuntaAPunta` construye un `YoloxDetector` sin cargar el modelo
(`object.__new__` + una `FakeSession`) y le hace devolver una salida cruda con
**una caja en un anchor conocido**. Después verifica que esa caja vuelve al píxel
correcto del **frame original**, no del 416×416. Cubre preprocess → sesión →
decode → NMS → des-letterbox de una sola vez, es determinista y corre en
milisegundos.

### El test de integración, ahora con inferencia

Pasó de 1 test que no inferia nada a **10 parametrizados** sobre los 5
analizadores con sus modelos reales, en 4 s. Las aserciones son estructurales a
propósito — fijar detecciones concretas sería frágil entre versiones de
onnxruntime, pero una caja fuera del frame o una confianza fuera de rango es
inequívocamente un bug nuestro. Más un test de **determinismo**: dos instancias
limpias, el mismo frame, el mismo resultado (se comparan dos instancias y no dos
llamadas porque los analizadores guardan estado: fondo de MOG2, tracks,
histéresis de puerta).

### El bug de `model_assets`, cerrado con evidencia

Se había reproducido esta mañana en el smoke de la Fase 0: con `AUREA_DATA_DIR`
redirigido, los dos modelos se bajaban de internet, contra la promesa explícita
del docstring de que *"la demo puede correr SIN internet"*. La causa:
`bundled_path` resuelve contra `PROJECT_ROOT` y los `.onnx` versionados viven en
`PROJECT_ROOT/data/models/`.

`ensure_model` prueba ahora **dos candidatos**, el del bundle y el del repo. Lo
pagaba también el CI: `build-windows.yml` corre el smoke con un data-dir vacío
en cada ejecución. Antes y después, mismo comando:

```
antes:   [WARNING] Modelo yolox_tiny.onnx ausente: descargando de https://github.com/...
después: [INFO]    Modelo yolox_tiny.onnx copiado desde /home/dani/work/AureaIA/data/models
```

### T4 — la cadena muerta, y lo que destapó al irse

Se fueron `refresh_device_status`, `test_rtsp_connection` y `_open_and_read`.
**Corrección al informe del 21/09**: ese informe decía que `refresh_device_status`
era el único emisor de `event_bus.device_status`. No lo era —
`stream_manager._report_status` también lo emite y `device_management.py:160` lo
consume. Lo muerto era solo esa cadena de tres.

El efecto estructural lo destapó ruff: sin ella, `device_manager` **deja de
importar `repository`, `event_bus` y `DeviceStatusEvent`**. O sea que el módulo
pasó de "hace I/O de red y además escribe en la DB y publica en el bus" a I/O de
red pura. Hay un test que lo fija: si vuelve a aparecer uno de esos imports,
alguien le está metiendo persistencia adentro.

De paso cayó el último ítem de "Robustez": los hilos daemon de `grab_snapshot`
ya tienen techo (`MAX_CONCURRENT_PROBES = 4`). El detalle que importa es **quién
libera la ranura**: la libera el propio thread de sondeo, no el caller, porque
el thread sobrevive al timeout de `grab_snapshot` — el backend FFmpeg tarda ~30 s
contra un host inalcanzable pese a los timeouts configurados, y eso ya estaba
documentado en el código desde antes.

### Los tests discriminan (se verificó revirtiendo cada pieza)

| Pieza revertida | Test que falla |
|---|---|
| Letterbox centrado en vez de arriba-izquierda | `test_el_relleno_gris_va_abajo_y_a_la_derecha`, `test_la_escala_deshace_el_letterbox` |
| Grilla con x e y invertidos | `test_la_grilla_avanza_primero_en_x` |
| NMS que no suprime entre clases | `test_es_class_agnostic` |
| Un solo candidato en `ensure_model` | `test_con_el_data_dir_redirigido_toma_el_modelo_del_repo` |
| Techo de sondas sacado | `test_con_las_ranuras_tomadas_no_abre_otra_conexion` |

### Verificación

| Chequeo | Resultado |
|---|---|
| `ruff check aurea_vms tests tools` | ✅ |
| `ruff format --check` | ✅ 133 archivos |
| `pytest -m "not integration" --cov` | ✅ **370 pasan**, cobertura **89.83%** |
| `pytest -m integration -v` | ✅ **10 pasan** (eran 1), 4 s, con inferencia real |
| `--smoke` con data-dir vacío | ✅ copia del repo, **sin descargar** |
| Tests contra las piezas revertidas | ✅ 6 fallan (ver tabla) |

Commit 0 cerró en 334 tests / 84.51%. Ahora **370 / 89.83%** (+36 tests, +5,3
puntos). Por módulo: `object_detector_backend` **40% → 95%**, `device_manager`
**44% → 65%**, `model_assets` al 100%.

### Archivos tocados

- `aurea_vms/core/analytics/model_assets.py` — segundo candidato y docstring.
- `aurea_vms/config/resources.py` — el docstring documentaba el bug.
- `aurea_vms/core/device_manager.py` — se va la cadena muerta, entra el techo de
  sondas, `CONNECTION_TEST_TIMEOUT_S` pasa a `SNAPSHOT_TIMEOUT_S`.
- `aurea_vms/ui/modules/device_management.py` — el docstring prometía una
  "prueba de conexión" que no existía.
- `tests/test_yolox_pipeline.py` y `tests/test_model_assets.py` — nuevos.
- `tests/test_registry.py`, `tests/test_device_manager.py`.
- `docs/ROADMAP.md` — cuatro ítems a "Hecho", "Robustez" queda sin ítems
  abiertos, y entra un hueco de producto nuevo: una cámara que nadie abre se
  queda en "Sin probar" para siempre.

---

## Fase 7 — Lógica de dominio fuera de los widgets (T2 + T3 + T6)

### T2 — El QListWidget era el modelo de datos

La galería de rostros guardaba cada candidato en el `Qt.ItemDataRole.UserRole`
de su item y el algoritmo leía y escribía la lista de la UI. No había una lista
Python paralela: **el widget de Qt era la estructura de datos**. Consecuencias
concretas: el algoritmo de identidad — el corazón del panel — no tenía un solo
test, quedaba fuera del scope de cobertura por vivir en `ui/`, y
`_apply_daily_reset` usaba `dt.datetime.now()` directo, o sea era intesteable.

`core/face_catalog.py` es ahora dueño de las capturas, el dedup por umbral, la
asignación de `track_id`, el reemplazo por área y el reinicio diario (con el
reloj inyectable). El widget quedó como renderer: aplica el `CatalogUpdate` que
le devuelve el catálogo. Las dos listas van en el mismo orden — más reciente
primero — y por eso los índices coinciden; el `removed_index` se aplica **antes**
de insertar en la posición 0, que es el orden en que el catálogo modificó la
suya.

`face_gallery.py` bajó de 315 a 204 líneas y dejó de importar numpy.

### Un traspié que vale documentar: el ruido aleatorio no sirve de "cara"

La primera versión de los tests fabricaba caras distintas con
`np.random.default_rng(semilla)`. Seis tests fallaban, y no por el código:
`face_signature` **ecualiza el histograma**, y eso aplana cualquier ruido
aleatorio a una distribución uniforme. Dos ruidos independientes terminan a
~0.33 de distancia media absoluta — justo por debajo del umbral por defecto de
0.35. O sea que "dos caras distintas" armadas con ruido matchean como la misma
persona.

Los tests de identidad usan ahora firmas sintéticas explícitas (distancia
exacta, umbral 0.05) y `face_signature` se prueba aparte con recortes reales.
Un test dice lo que quiere decir o no sirve.

### T3 — No era sólo rendimiento: había dos bugs de corrección

`event_dashboard.refresh()` traía 200 eventos y todos los dispositivos cada 5 s
para contar en Python. Al mirarlo de cerca aparecieron dos errores:

1. La tarjeta **"Eventos registrados" mostraba `len()` de la página de 200**, o
   sea 200 para siempre en cuanto hubiera más eventos que eso.
2. Con el **filtro global de sitio activo**, se traían los últimos 200
   **globales** y recién después se descartaban en Python los de otros sitios.
   Resultado: la tabla mostraba un subconjunto arbitrario en vez de los últimos
   200 de ese sitio. Con 500 eventos de una cámara de Anexo sepultando 5 de
   Sala, Sala mostraba 5 filas o ninguna según dónde cayera el corte.

El repositorio gana `count_alarm_events()` (filtros de cámara, sitio, severidad
y estado), `count_devices_by_status()` y los filtros `site_id`/`device_id` en
`list_alarm_events`. El filtro por sitio es un JOIN de dos saltos (evento →
cámara → zona → sitio). De paso `list_devices(site_id=…)` pasó de dos consultas
a un JOIN: traía todas las `Zone` del sitio a Python para armar un `IN (...)`.

Y `count_pending_alarm_events` dejó de tener el literal `"resuelta"`
hardcodeado: usa `STATUS_RESOLVED`.

### T6 — La única fuga del ORM a la UI

`ui/dialogs/device_dialog.py` importaba `sqlalchemy.exc.IntegrityError` para
poder decir "Ya existe un sitio llamado X". Era el único import de SQLAlchemy en
toda la capa de widgets, y ataba el diálogo al ORM justo en un proyecto diseñado
para poder cambiar de motor.

`models/errors.py` trae `RepositoryError` y `DuplicateError`, y `repository`
traduce en su frontera con `_escritura()`. El chequeo mira las dos frases —
`UNIQUE constraint failed` de SQLite y `duplicate key` de Postgres — porque el
punto es no quedar atado a un motor.

```
$ grep -rn sqlalchemy aurea_vms/ui/
NINGUNA
```

Hay un test que recorre `aurea_vms/ui/**/*.py` y falla si alguien vuelve a
importar el ORM en un widget: lo caza el CI, no la revisión.

**Efecto lateral de limpieza**: los siete `add_*` y los seis `update_*` eran
trece copias del mismo bloque de cinco líneas. Ahora comparten `_insert` y
`_update`, que es también donde vive la traducción de excepciones. Esa frontera
única es sobre la que se apoya la whitelist de kwargs de la Fase 9.

### Los tests discriminan

| Pieza revertida | Tests que fallan |
|---|---|
| `combined_difference` promediando en vez de tomar el máximo | 1 |
| Reemplazo por área que se queda siempre con la entrante | 1 |
| Reinicio diario sin recordar que ya reinició hoy | 2 |
| Filtro de sitio sacado de las consultas de eventos | 4 |
| Frontera de excepciones sin traducir | 6 |

14 en total.

### Verificación

| Chequeo | Resultado |
|---|---|
| `ruff check aurea_vms tests tools` | ✅ |
| `ruff format --check` | ✅ 138 archivos |
| `pytest -m "not integration" --cov` | ✅ **432 pasan**, cobertura **90.50%** |
| `pytest -m integration` | ✅ 10 pasan |
| `python -m aurea_vms.main --smoke` | ✅ los 4 analizadores |
| Tests contra los fixes revertidos | ✅ 14 fallan (ver tabla) |

Fase 6 cerró en 370 tests / 89.83%. Ahora **432 / 90.50%** (+62 tests).

### Archivos tocados

- `aurea_vms/core/face_catalog.py` y `aurea_vms/models/errors.py` — nuevos.
- `aurea_vms/ui/widgets/face_gallery.py` — de 315 a 204 líneas, sin numpy.
- `aurea_vms/models/repository.py` — agregados, filtros, `_insert`/`_update` y
  la frontera de excepciones.
- `aurea_vms/ui/modules/event_dashboard.py`, `aurea_vms/ui/widgets/dashboard_panel.py`.
- `aurea_vms/ui/dialogs/device_dialog.py` — captura `DuplicateError`.
- `tests/test_face_catalog.py`, `tests/test_repository_agregados.py`,
  `tests/test_repository_errores.py` — nuevos. `tests/test_sites.py`.
- `docs/ROADMAP.md` — tres ítems a "Hecho"; queda anotado que la galería
  **sigue calculando en el hilo de la GUI**, que ahora es barato de mover.
